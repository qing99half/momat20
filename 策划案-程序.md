# 《母亲二十岁》程序策划案

> **面向人员**: 清九(强模型操作员) / 陈洒(弱模型操作员)  
> **用途**: 零编程基础通过 Claude Code 完成游戏开发的操作手册  
> **核心原则**: 教你如何与 AI 协作，而非如何编程

---

## 开篇必读:这个游戏讲了什么故事

**请先理解剧情，再开始干活。** 你要做的每一个系统都在为这个故事服务。

### 剧情白话版

一个中年女儿在老房子里发现了妈妈年轻时的日记本，笔记本被锁住了。她撬开锁，被吸入日记构成的记忆世界——

**第一章(母亲年轻的故事)**:玩家操控一个年轻女性，闯过四关痛苦记忆(家暴/下岗/渣男/育儿)。玩家以为"这是女儿自己的童年记忆"。

**第二章(女儿拯救)**:同样的四关变得破败，玩家获得冲刺能力，以为在"拯救过去的自己"。每关读一篇日记，日记的**日期是1990年代**(比女儿的童年早一代人)——这是全游戏唯一的反转证据。

**第三章(相遇)**:玩家操控女儿只能向左慢走，迎面走来一个年老的母亲。两人在一扇门前相遇，共同推门，白光中响起《世上只有妈妈好》。

**反转**: 日记不是女儿的，是**妈妈二十岁时写的**。那些苦难是妈妈的人生。女儿以为在拯救自己，其实在走妈妈的路。

### 为什么要误导玩家

- 年轻母亲和女儿用**同一套角色素材**(玩家自然以为是同一个人)
- 第一章不显示章节标题(任何"母亲"字样都会提前暴露)
- 日记在第二章才能读，日期写在最显眼位置——**1993年、1996年、1997年、1999年**
- 评委看到日期的瞬间会"咯噔"一下:这不是2000年代的孩子，这是上一代人

### 你的工作目标

做出一个15~20分钟的横版平台跳跃游戏，手感像《蔚蓝》，叙事靠环境和演出(几乎无文本)，最后一章推门时用《世上只有妈妈好》童声清唱当泪点引爆。

**48小时完成，两人分工明确，AI是你的手，策划案是你的眼睛。**

---

## 项目概述与分工

### 团队配置

- **清九**: 强模型操作员，负责玩法内核(手感、陷阱、节拍器、死亡重生)
- **陈洒**: 弱模型操作员，负责流程与表现(状态机、摄像机、UI、关卡搭建)
- **音乐成员**: 人工制作音乐(M1/M2/M3/V1)，兼看管第三台电脑跑 AI 出图

### 技术栈

- 引擎: **Godot 4.7.2(.NET 构建)** (由 4.6 升级;注意不是 4.3，4.4~4.7 很多 API 变了,写码前必读附录 A 过时写法黑名单)
- 语言: **GDScript**(用 .NET 构建但**脚本一律 GDScript,不写 C#**——AI 可靠生成 GDScript、无编译负担;详见 `docs/engine-reference/godot/VERSION.md`)
- 4.7 利好: 原生支持任意方向单向碰撞,单向平台无需手写
- 分辨率: 游戏层 320×180 整数倍放大，UI 层原生分辨率
- 版本控制: Git，trunk-based，**禁止同时改同一文件**

### 接口约定(H0阶段,写死不改)

**全局事件总线** `EventBus.gd` (autoload):

```gdscript
# 六个信号，清九只发、陈洒只听，零直接调用
signal player_died
signal level_completed(level_id: String)
signal beat(beat_number: int)
signal dash_unlocked
signal diary_finished
signal door_opened
```

**资源命名规范**:

- 背景: `bg_ch1_lv1.png` (章_关)
- 陷阱: `trap_pendulum.png` (类型)
- 音乐: `M1_piano.ogg` / `M2_strings.ogg`
- 关卡: `level_ch1_lv1.tscn`

**Git 分工红线**:

- 清九只动: `src/player/` `src/traps/`
- 陈洒只动: `src/flow/` `src/ui/` 场景文件
- 共享资源修改前必须口头通知对方

### 48小时时间线

| 时间 | 清九 | 陈洒 |
|---|---|---|
| **H0~2** | 环境+接口(共同) | 环境+接口(共同) |
| **H2~8** | 手感调试(白盒度量关) | 流程状态机+LUT shader |
| **H8~16** | 陷阱基类+9种陷阱 | 日记UI+眼睑过场+摄像机 |
| **H16~24** | Conductor节拍器+同拍 | 一章4关TileMap搭建 |
| **H24** | 里程碑①:一章可玩 | 里程碑①:一章可玩 |
| **H24~36** | 二章陷阱密度调整+冲刺 | 二章破败化+三章推门 |
| **H36** | 里程碑②:全流程可穿 | 里程碑②:全流程可穿 |
| **H36~44** | 音效接入+BUG修复 | UI润色+OP/ED接入 |
| **H44~48** | 封版打包 | 封版打包 |

---

## 公共前缀块(所有提示词开头)

> 以下190字的前缀块，**每条提示词都要连它一起粘贴给 Claude Code**。它告诉 AI 项目的硬约束。

```
【项目锚点·全部写死不许动】
引擎 Godot 4.7.2(.NET 构建)+ GDScript(不写 C#);写代码前先读 docs/engine-reference/godot/VERSION.md 与附录 A(过时写法黑名单),禁止 Godot 4.6 以前的过时写法。
信号只用 EventBus(autoload)已有的六个:player_died / level_completed(id) / beat(n) / dash_unlocked / diary_finished / door_opened,不许新增或改名。
目录:只改【本条注明的目录】,其余只读。
单位:1格=20px(2026-08-29 起,主角20×20基准),数值一律照抄提示词,不许自改。
禁止:重构现有代码、改场景树结构、新增 autoload/插件、用占位冒充完成。
```

**注意**: 每条任务提示词会把【本条注明的目录】替换成具体路径(如 `src/player/` 或 `src/ui/`)。

---

## H0~2:环境与接口搭建(共同完成)

### 任务0.1:创建项目目录结构(陈洒执行)

直接在 Godot 项目根目录下创建以下文件夹:

```
src/
  player/
  traps/
  flow/
  ui/
assets/
  data/
  sprites/
  audio/
design/
docs/
tests/
production/
```

**无需提示词，手动创建即可。**

> ⚠ 额外创建 `assets/placeholder/`、`assets/shaders/`、`assets/tilesets/`、`src/levels/` 四个目录(后续任务会用到)。

### 任务0.1b:创建占位资源(陈洒执行)

**为什么**:美术老师第一天上午才交定妆图,AI 真素材要 H4 之后。在那之前用彩色方块占位跑通。**完整占位全表 + 替换约定见〔附录 E〕**,先按那张表把占位建好再往下做。三条铁律:①占位放 `assets/placeholder/` 单独文件夹;②占位文件名=`placeholder_`+真素材名,**尺寸必须和真素材一致**;③每个方块带文字标签。

### 任务0.2:创建EventBus全局信号(清九执行)

#### 清九版提示词

```
【项目锚点·全部写死不许动】
引擎 Godot 4.7.2(.NET 构建)+ GDScript(不写 C#);写代码前先读 docs/engine-reference/godot/VERSION.md 与附录 A(过时写法黑名单),禁止 Godot 4.6 以前的过时写法。
信号只用 EventBus(autoload)已有的六个:player_died / level_completed(id) / beat(n) / dash_unlocked / diary_finished / door_opened,不许新增或改名。
目录:只改【src/】,其余只读。
单位:1格=20px(2026-08-29 起,主角20×20基准),数值一律照抄提示词,不许自改。
禁止:重构现有代码、改场景树结构、新增 autoload/插件、用占位冒充完成。

【背景】
全游戏的系统通信靠一个全局事件总线,所有信号在这里定义。

【任务】
在 src/ 下创建 EventBus.gd,定义以下六个信号:
- player_died (玩家死亡)
- level_completed(level_id: String) (关卡完成)
- beat(beat_number: int) (音乐节拍)
- dash_unlocked (冲刺解锁)
- diary_finished (日记读完)
- door_opened (门打开)

然后在 Project Settings > Autoload 注册为全局单例,名称 EventBus。

【硬约束】
- 文件路径: src/EventBus.gd
- 信号名一字不改
- 无需任何逻辑,只声明信号

【验收】
完成后依次做四件事:
① 自测:打开 Godot,菜单 Project > Project Settings > Autoload,能看到 EventBus。
② 教我验证:告诉我"打开 Project Settings > Autoload,列表里有 EventBus 就对了"。
③ 未完成清单:列所有 TODO/占位/简化;没有就原话回复"本任务无占位"。
④ 我回复"通过"之前,不要开始任何下一步。
```

**验收标准**: Autoload 列表里出现 EventBus,路径指向 src/EventBus.gd。

**如果不对**: 把报错原文粘给 AI,说"修复它"。

---

## H2~24:清九任务线(玩法内核)

### 任务1:白盒度量关与手感调试(H2~8)

#### 清九版提示词

```
【项目锚点·全部写死不许动】
引擎 Godot 4.7.2(.NET 构建)+ GDScript(不写 C#);写代码前先读 docs/engine-reference/godot/VERSION.md 与附录 A(过时写法黑名单),禁止 Godot 4.6 以前的过时写法。
信号只用 EventBus(autoload)已有的六个:player_died / level_completed(id) / beat(n) / dash_unlocked / diary_finished / door_opened,不许新增或改名。
目录:只改【src/player/】,其余只读。
单位:1格=20px(2026-08-29 起,主角20×20基准),数值一律照抄提示词,不许自改。
禁止:重构现有代码、改场景树结构、新增 autoload/插件、用占位冒充完成。

【背景】
这是横版平台跳跃游戏的核心:角色控制器。手感必须像《蔚蓝》——响应快、宽容度高、摔不死。

【任务】
创建玩家角色 CharacterBody2D,实现以下手感参数(蔚蓝移植):
- 移动:6帧到全速,3帧刹停,转向无打滑,空气摩擦大
- 跳跃:净空3~3.5格(1格=20px),可变跳高(按住更高),顶点滞空感
- 土狼时间:离开平台后0.15s内仍可跳
- 跳跃缓冲:落地前0.1s按跳跃,落地瞬间自动跳
- 判定内缩:碰撞体比精灵小5px,擦边不死
- 撞角修正:斜角碰撞自动修正到平台边缘
- 物理:CharacterBody2D + 手写velocity,禁止RigidBody2D

创建测试场景 tests/whitebox_measure.tscn:
- 20×20格子网格背景(用ColorRect画格子线)
- 标注跳跃高度(3格线)、跳跃距离(4~5格平台间距)
- 最宽安全缺口、连续小平台、斜坡

【硬约束】
- 碰撞体:20×20px(1×1格,主角=格基准),底部居中锚点
- 重力/速度单位:像素/秒
- move_and_slide() 无参数(Godot 4.7 写法)
- 必须能精确跳过3格宽缺口、落到1格宽平台

【验收】
① 自测:F5运行测试场景,方向键移动、空格跳跃,能精确跳3格缺口。
② 教我验证:告诉我"按F5,用方向键和空格,能稳定跳过3格宽的缺口就对了。如果跳不过或者卡住了就是参数不对"。
③ 未完成清单:逐条列出哪些宽容机制没实现、哪些是估算值。
④ 等我回复"通过"。
```

**验收标准(你来判断)**:
- 按F5能看到格子网格和平台
- 角色能左右移动、跳跃
- 能跳过3格宽的缺口(60px)
- 离开平台边缘后短时间内按空格仍能跳起(土狼时间)
- 落地前按空格,落地瞬间自动跳(跳跃缓冲)

**如果卡住**: 
1. 先让AI重试一次
2. 仍然卡住,新开对话,把红色报错完整粘贴,说"这是Godot 4.7.2,你可能用了旧版API,对照docs/engine-reference/godot/VERSION.md和附录A修复"
3. 超30分钟仍解决不了,立即叫清九接手

---

### 任务2:陷阱基类与9种陷阱实现(H8~16)

#### 清九版提示词

```
【项目锚点·全部写死不许动】
引擎 Godot 4.7.2(.NET 构建)+ GDScript(不写 C#);写代码前先读 docs/engine-reference/godot/VERSION.md 与附录 A(过时写法黑名单),禁止 Godot 4.6 以前的过时写法。
信号只用 EventBus(autoload)已有的六个:player_died / level_completed(id) / beat(n) / dash_unlocked / diary_finished / door_opened,不许新增或改名。
目录:只改【src/traps/】,其余只读。
单位:1格=20px(2026-08-29 起,主角20×20基准),数值一律照抄提示词,不许自改。
禁止:重构现有代码、改场景树结构、新增 autoload/插件、用占位冒充完成。

【背景】
9种陷阱复用同一套架构:运动层(Tween/代码)+ 材质层(帧动画)+ 反馈层(粒子)。

【任务】
创建 TrapBase.gd 基类:
- Area2D,监听 body_entered
- 加载配置资源(TrapConfig)
- 统一信号:trap_activated / trap_hit_player
- 预警机制:危险动作前2帧白闪(shader)

实现9种陷阱类(都继承TrapBase,按行为分类不按美术名):
1. **SpikeStrip**(静态尖刺带):搓衣板/零件/荆棘/玻璃渣 = 换贴图+判定尺寸,同一个类
2. **ForceZone**(外力区域):传送带(顺向推动,可旋转=升降带),damage可为0(账单纸风已删,实现难度太高)
3. **MovingHazard**(位移轨迹):鸡毛掸子摆锤(正弦旋转±60°,周期2.0s)、酒瓶坠落(下落5格)
4. **TimedHazard**(周期激活):冲压机(周期2.5s)、腐心平台(踩后1s碎裂3s重生)、巨心滴液、啼哭声波

每个陷阱的参数(周期/幅度/判定体/预警时间)从 TrapConfig 资源读取,不硬编码。

【硬约束】
- 禁止每个陷阱美术名一个脚本(不建WashboardTrap/GlassTrap)
- 陷阱→玩家接口:player.receive_hazard(knockback, source_id)
- 判定体普遍小于贴图5px("擦尖不死"),**全部致死陷阱统一 hitbox_inset=5px 写进基类**
- **预警是两层不是一句**:①warn_duration=持续态预警≥0.5s(蒸汽/阴影/摇晃/收缩,播对应动效)②flash_frames=2=击发前2帧白闪(强调,非预警本体)。严禁压成"预警=前2帧白闪"(2帧=33ms物理上不可反应)

【验收】
① 自测:在测试场景放一个摆锤+一个尖刺带,玩家触碰后发射player_died信号。
② 教我验证:告诉我"在测试场景按F5,碰到摆锤或尖刺应该触发死亡(控制台打印player_died)。如果碰了没反应或者没碰就死了,判定体有问题"。
③ 未完成清单:列出哪些陷阱是占位/哪些参数暂时硬编码。
④ 等我说"通过"。
```

**验收标准**:
- 能在场景里拖一个陷阱节点
- 玩家碰到陷阱触发死亡(控制台看到 player_died 信号打印)
- 摆锤会左右摆动,危险前有白闪预警

**如果不对**: 描述现象(碰了没反应/没碰就死/不会动),粘给AI说"修复判定体/运动逻辑"。

---

### 任务3:Conductor节拍器与陷阱同拍(H16~24)

#### 清九版提示词

```
【项目锚点·全部写死不许动】
引擎 Godot 4.7.2(.NET 构建)+ GDScript(不写 C#);写代码前先读 docs/engine-reference/godot/VERSION.md 与附录 A(过时写法黑名单),禁止 Godot 4.6 以前的过时写法。
信号只用 EventBus(autoload)已有的六个:player_died / level_completed(id) / beat(n) / dash_unlocked / diary_finished / door_opened,不许新增或改名。
目录:只改【src/flow/】,其余只读。
单位:1格=20px(2026-08-29 起,主角20×20基准),数值一律照抄提示词,不许自改。
禁止:重构现有代码、改场景树结构、新增 autoload/插件、用占位冒充完成。

【背景】
音乐120BPM,陷阱周期必须是0.5s整数倍(= 音乐一拍),形成可读节奏。

【任务】
创建 Conductor.gd:
- **挂 3 个同步的 AudioStreamPlayer 节点**(M1/M2 各有钢琴/环境/节奏三分轨,四关靠开关分轨调浓度,D-401;三轨同起点同长度,只调各自音量不改播放位置)
- 从主音轨(钢琴轨)播放位置计算当前拍数:beat = (钢琴轨.get_playback_position() + offset) * BPM / 60.0
- 每拍发射 EventBus.beat(beat_number)
- 状态机:STOPPED / PLAYING / PAUSED / LOOPING
- 暂停时保存beat,恢复时续播,防止拍号跳变
- 循环点处理:beat回卷时先结算旧循环剩余事件再重置

陷阱订阅 EventBus.beat 信号,按配置的 active_beats / cooldown_beats 激活/休眠。

【硬约束】
- BPM = 120.0(精确浮点)
- 周期陷阱period必须 ∈ {1.0, 1.5, 2.0, 2.5, 3.0} 秒
- beat比较用epsilon=0.05(浮点容差)
- 循环点边界事件不重复/不漏触发

【验收】
① 自测:播放测试音频(120BPM),控制台每拍打印beat信号,拍号连续无跳变。
② 教我验证:告诉我"播放音乐,控制台应该每0.5秒打印一次beat(0, 1, 2...),暂停再播数字接上不跳就对了"。
③ 未完成清单:循环点逻辑/暂停恢复是否完整实现。
④ 等我说"通过"。
```

**验收标准**:
- 播放音乐,控制台每0.5秒打印一次 beat(数字递增)
- 按暂停键,beat停止增长
- 恢复播放,beat从暂停的数字继续,不跳变

**如果不对**: 粘贴控制台输出,说"beat数字跳变了"或"暂停后beat重置了"。

---

### 任务4:死亡与重生系统(H16~24)

#### 清九版提示词

```
【项目锚点·全部写死不许动】
引擎 Godot 4.7.2(.NET 构建)+ GDScript(不写 C#);写代码前先读 docs/engine-reference/godot/VERSION.md 与附录 A(过时写法黑名单),禁止 Godot 4.6 以前的过时写法。
信号只用 EventBus(autoload)已有的六个:player_died / level_completed(id) / beat(n) / dash_unlocked / diary_finished / door_opened,不许新增或改名。
目录:只改【src/player/, src/traps/】,其余只读。
单位:1格=20px(2026-08-29 起,主角20×20基准),数值一律照抄提示词,不许自改。
禁止:重构现有代码、改场景树结构、新增 autoload/插件、用占位冒充完成。

【背景】
触陷阱=像素碎裂粒子+1秒后台灯旁重生,无生命值、无黑屏、无惩罚。

【任务】
实现统一死亡流程(按顺序执行):
1. 玩家触陷阱 → 发射 EventBus.player_died
2. 冻结输入,播放碎裂粒子(GPUParticles2D,像素方块向四周飞散)
3. 玩家sprite隐藏
4. 停止Conductor逻辑拍(音乐继续,节拍事件不触发)
5. 重置所有陷阱到初始态(位置/相位/Timer.stop/Tween.kill)
6. 等待1秒
7. 玩家传送回最近检查点(台灯位置)
8. 碎裂粒子倒放=重生聚合
9. 玩家sprite显示,恢复输入

检查点=昏黄小台灯(2态贴图:灭/亮),经过时"啪"亮起+音效。
每关3盏:起点1盏+核心段入口1盏+组合考试段入口1盏(即死系统下压低最坏重跑到20~25s)。

【硬约束】
- 无生命值、无死亡计数、无Game Over画面
- 粒子:20×20px方块,初速500~1000,重力750,寿命0.8s
- **无无敌帧系统**(D-537 已删血量/无敌/受伤态);仅在重生后 0.5s 内临时 `can_die=false` 防"重生瞬间与陷阱相位重叠即死"的连死循环,不等于无敌机制
- **配套关卡规则:台灯前后 5 格内不放动态陷阱**(由 TileMap 布局保证,与重生保护共同防连死)
- 检查点保存:位置+朝向
- 死亡期间can_die=false(防止连续触发)

【验收】
① 自测:碰陷阱,角色碎裂消失,1秒后在台灯旁聚合重生。
② 教我验证:告诉我"碰陷阱后应该看到:角色碎成像素块飞散→1秒后在台灯旁聚合出现。如果没粒子或者卡住不动了就有问题"。
③ 未完成清单:粒子效果/音效/陷阱重置是否完整。
④ 等我说"通过"。
```

**验收标准**:
- 碰陷阱,角色消失,像素块飞散
- 1秒后角色在台灯旁重新出现
- 陷阱回到初始位置(摆锤回到起始角度)
- 能继续操作,不卡死

**如果不对**: 描述卡在哪步(碎了不重生/重生了陷阱没重置/重生后不能动)。

---

## H2~24:陈洒任务线(流程与表现)

### 任务5:流程状态机搭建(H2~8,分7小步)

**陈洒专用:每步一条提示词,做完一步看到对了再喂下一步。**

#### 步骤5.1:创建GameState状态机

```
【项目锚点·全部写死不许动】
引擎 Godot 4.7.2(.NET 构建)+ GDScript(不写 C#);写代码前先读 docs/engine-reference/godot/VERSION.md 与附录 A(过时写法黑名单),禁止 Godot 4.6 以前的过时写法。
信号只用 EventBus(autoload)已有的六个,不许新增或改名。
目录:只改【src/flow/】,其余只读。
单位:1格=20px(2026-08-29 起,主角20×20基准),数值一律照抄提示词,不许自改。
禁止:重构现有代码、改场景树结构、新增 autoload/插件、用占位冒充完成。

在 src/flow/ 下创建 GameState.gd,定义四个状态的枚举:
enum State { Gameplay, Cutscene, Transition, Menu }

再定义一个变量 current: State = State.Gameplay

只执行本条指令,不要做本条没提到的任何事,做完就停下汇报。
```

**【你应该看到】**: 文件 src/flow/GameState.gd 被创建,里面有 enum State 和 current 变量。

**【如果不对】**: 把红色报错粘给AI,说"修复它"。

---

#### 步骤5.2:注册GameState为Autoload

```
【项目锚点】(同上,省略)

打开 Project Settings > Autoload,添加 src/flow/GameState.gd,命名为 GameState,勾选 Enable。

只执行本条指令,做完就停下汇报。
```

**【你应该看到】**: Project Settings > Autoload 列表里出现 GameState。

**【如果不对】**: 手动打开 Project > Project Settings > Autoload,点"+"号,选 src/flow/GameState.gd,Name填GameState,勾Enable,点Add。

---

#### 步骤5.3:创建双层渲染结构

```
【项目锚点】(同上,省略)

创建主场景 src/MainGame.tscn:
- 根节点 Node2D
- 子节点1: SubViewportContainer(名称:GameLayer)
  - 子节点: SubViewport(尺寸320×180,Stretch开启)
- 子节点2: CanvasLayer(名称:UILayer)

SubViewport 将来放游戏内容(角色/背景/陷阱),CanvasLayer 放 UI(日记/文字)。

只执行本条指令,做完就停下汇报。
```

**【你应该看到】**: 场景树里有 MainGame > GameLayer(SubViewportContainer) > SubViewport 和 MainGame > UILayer(CanvasLayer)。

**【如果不对】**: 手动创建:右键场景根Add Child Node,搜SubViewportContainer,创建后在它下面Add Child Node搜SubViewport,点SubViewport在Inspector设置Size=320×180。

---

#### 步骤5.4:摄像机跟随角色

```
【项目锚点】(同上,省略)

在玩家角色场景里添加 Camera2D 子节点:
- Position Smoothing Enabled = true
- Position Smoothing Speed = 10.0
- Limit Left = 0
- Limit Right = 2600 (每关约130格×20px,阻-02;各关按实际布局末端+半屏微调,不留地图外空白)
- Limit Top = 0 (关卡顶=视野顶,地面上方16格)
- Limit Bottom = 360

摄像机跟随角色,只横移,垂直锁定(单屏400×360=20×18格=整关高度,开局即见关卡顶,无垂直滚动)。

只执行本条指令,做完就停下汇报。
```

**【你应该看到】**: 角色场景里有 Camera2D 节点,Inspector 里能看到 Position Smoothing 和 Limit 参数。

**【如果不对】**: 打开玩家场景,右键角色根节点Add Child,搜Camera2D,在Inspector设置上面那些数字。

---

#### 步骤5.5:OP视频播放器

```
【项目锚点】(同上,省略)

创建 src/flow/OPScene.tscn:
- 根节点 Control(Anchor/Margin全屏)
- 子节点 VideoStreamPlayer:
  - Stream = (等视频文件就绪后设置)
  - Autoplay = true
  - 播放结束信号 finished 连接到脚本

脚本:播放结束后加载游戏主场景,长按任意键1秒可跳过(用Timer检测)。

只执行本条指令,做完就停下汇报。
```

**【你应该看到】**: 场景里有 VideoStreamPlayer 节点。

**【如果不对】**: 先创建,视频文件还没有就暂时不设置Stream,脚本先写"播放结束print('OP finished')"占位。

---

#### 步骤5.6:主菜单场景

```
【项目锚点】(同上,省略)

创建 src/ui/MainMenu.tscn:
- 根节点 Control(全屏)
- 背景 TextureRect(Texture = ui_menu_bg.png,等素材)
- 标题 Label(Text="母亲二十岁",字体Fusion Pixel 48px,居中上方)
- 提示 Label(Text="按任意键开始",24px,居中下方,颜色呼吸闪烁)

按任意键进入OP场景。

只执行本条指令,做完就停下汇报。
```

**【你应该看到】**: 场景里有背景图(如果素材没到就用ColorRect占位)、两个Label。

**【如果不对】**: 手动拖节点,Label的Text在Inspector里填。

---

#### 步骤5.7:眼睑过场shader

```
【项目锚点】(同上,省略)

创建 src/ui/EyelidTransition.tscn:
- 根节点 ColorRect(黑色,全屏,初始visible=false)
- 用 Tween 控制上下两个黑色矩形从屏幕上下边缘向中间合拢(闭眼),或从中间向边缘打开(睁眼)

提供两个方法:
- close_eyes(duration: float) # 闭眼
- open_eyes(duration: float) # 睁眼

只执行本条指令,做完就停下汇报。
```

**【你应该看到】**: 场景里有ColorRect,脚本里有close_eyes和open_eyes函数。

**【如果不对】**: 让AI重写一遍,说"用Tween让黑色矩形从上下边缘移动到中间,duration=1.0秒"。

**小结**: 7步完成,状态机+双层渲染+摄像机+OP/菜单+过场都有了。每步卡住超15分钟立即叫清九。

---

### 任务6:LUT调色shader(H8~12,分3小步)

#### 步骤6.1:LUT shader模板

```
【项目锚点】(同上,省略)

在 assets/shaders/ 下创建 lut_color_grading.gdshader:
uniform sampler2D lut_texture;

在fragment()函数里:
1. 读取屏幕颜色 screen_color
2. 用 screen_color.rgb 作为坐标查 lut_texture(256×16的LUT图)
3. 输出查到的新颜色

这是标准LUT调色shader,照Godot官方文档或Celeste实现写。

只执行本条指令,做完就停下汇报。
```

**【你应该看到】**: assets/shaders/lut_color_grading.gdshader 文件被创建。

**【如果不对】**: 把shader编译错误粘给AI,说"这是Godot 4.7.2,修复shader语法"。

---

#### 步骤6.2:给SubViewport挂shader

```
【项目锚点】(同上,省略)

在 MainGame.tscn 的 SubViewport 节点上:
1. 添加 BackBufferCopy 子节点(用于读屏幕颜色)
2. 在 SubViewport 的脚本里设置 environment,挂上 lut_color_grading.gdshader
3. 暴露参数 lut_texture,可以在运行时切换LUT图

只执行本条指令,做完就停下汇报。
```

**【你应该看到】**: SubViewport 节点Inspector里能看到shader参数。

**【如果不对】**: 让AI详细说明"如何在Godot 4.7.2给SubViewport应用后处理shader",然后重试。

---

#### 步骤6.3:测试LUT效果

```
【项目锚点】(同上,省略)

用测试LUT图(纯灰阶或反色)验证:
- 设置 lut_texture = 测试图
- F5运行,整个游戏画面应该变成灰蒙蒙或反色

确认shader生效,之后再换真正的情绪LUT。

只执行本条指令,做完就停下汇报。
```

**【你应该看到】**: 按F5,画面变灰或变反色(取决于测试LUT图)。

**【如果不对】**: 描述现象("画面没变化"或"黑屏"),粘给AI说"shader没生效,检查lut_texture是否正确传入"。

---

### 任务7:日记UI与翻页转场(H12~16,分5小步)

#### 步骤7.1:笔记本UI底图

```
【项目锚点】(同上,省略)

创建 src/ui/DiaryUI.tscn:
- 根节点 Control(全屏,初始visible=false)
- 背景遮罩 ColorRect(半透明黑,模糊背景)
- 笔记本图 TextureRect(Texture = ui_diary_open.png,居中,等素材)
- 日期 Label(右上角,24px)
- 正文 RichTextLabel(笔记本内,左页,支持逐字打出)

只执行本条指令,做完就停下汇报。
```

**【你应该看到】**: 场景树有上述节点,笔记本图如果没素材就用白色ColorRect占位。

**【如果不对】**: 手动拖节点,RichTextLabel要在Inspector勾选"BBCode Enabled"。

---

#### 步骤7.2:逐字打字机效果

```
【项目锚点】(同上,省略)

在 DiaryUI.gd 里实现:
- show_diary(date: String, text: String, chapter: int)
- 日期先逐字打出,打完前禁止跳过(D-505要求)
- 日期打完后,正文允许按键加速(visible_ratio跳到1.0)
- 打字速度:每字0.05秒
- 配音效:打字机"哒哒"声循环

只执行本条指令,做完就停下汇报。
```

**【你应该看到】**: 调用show_diary(),文字一个个蹦出来,日期打完前按键无效,打完后按键直接显示全文。

**【如果不对】**: 描述哪步不对("日期没逐字"或"按键没反应"),让AI单独修那部分。

---

#### 步骤7.3:记忆光片飞入HUD

```
【项目锚点】(同上,省略)

在 UILayer 右上角创建 HUD:
- 4个符号 ◇◇◇◇(空心=未获得,实心◆=已获得)
- 平时半透明(alpha=0.4)
- 获得瞬间:高亮闪烁+轻灵音效("叮")

DiaryUI关闭时,触发光片飞行:
- 从笔记本位置 Tween 飞到 HUD 对应符号
- 飞行时间0.8秒,带粒子轨迹(可选)
- 到达后符号从 ◇ 变 ◆

只执行本条指令,做完就停下汇报。
```

**【你应该看到】**: 日记关闭后,一个光点从屏幕中间飞到右上角,符号变实心。

**【如果不对】**: 描述看到什么("没飞行动画"或"符号没变"),让AI修Tween或符号切换逻辑。

---

#### 步骤7.4:翻页转场

```
【项目锚点】(同上,省略)

创建 src/ui/PageTurn.tscn:
- 纸页图 TextureRect(Texture = ui_page_turn.png,初始在屏幕右侧外)
- Tween 从右向左横扫全屏,遮住画面
- 扫到一半时切换场景(SceneTree.change_scene)
- 扫完后销毁自身
- 配翻页音效

提供方法:
- play_turn(next_scene_path: String)

只执行本条指令,做完就停下汇报。
```

**【你应该看到】**: 调用play_turn(),纸页从右向左扫过,画面切到下一关。

**【如果不对】**: 让AI详细说明"Tween横扫的起点终点坐标",确认是屏幕宽度1280。

---

#### 步骤7.5:幕间黑屏大字

```
【项目锚点】(同上,省略)

创建 src/ui/BlackscreenText.tscn:
- 根节点 ColorRect(纯黑全屏)
- Label(白色大字,居中,初始visible_ratio=0)
- 逐字浮现,每字0.1秒
- "妈妈"单独一屏,字号加倍,音乐骤停

提供方法:
- show_text(text: String, big: bool)

只执行本条指令,做完就停下汇报。
```

**【你应该看到】**: 调用show_text(),黑屏上文字一个个显示。

**【如果不对】**: 让AI重写,确认Label用的是Fusion Pixel字体,visible_ratio从0到1。

---

### 任务8:TileMap关卡搭建(H16~24,分4小步)

#### 步骤8.1:创建Tileset

```
【项目锚点】(同上,省略)

创建 assets/tilesets/platform_tileset.tres:
- 导入 20×20px 的平台tile图(brick_tile.png等素材)
- 设置碰撞形状(每个tile顶部有碰撞)
- Terrain自动瓦片(可选,如果有美术给的terrain set)

如果素材还没到,用纯色ColorRect占位,碰撞形状手动画20×20矩形。

只执行本条指令,做完就停下汇报。
```

**【你应该看到】**: TileSet资源被创建,能在TileMap编辑器里看到格子。

**【如果不对】**: 手动创建:新建TileSet资源,Add Tile,设置Texture,在Collision里画碰撞形状。

---

#### 步骤8.2:创建关卡模板

```
【项目锚点】(同上,省略)

创建 src/levels/LevelTemplate.tscn:
- 根节点 Node2D(名称:LevelRoot)
- 子节点 TileMapLayer(Godot 4.7 写法,不是旧 TileMap;Tileset设为上面的tileset)
- 子节点 ParallaxBackground(远景+中景两层)
- 子节点 SpawnPoints(Node2D,下面挂台灯检查点)
- 子节点 Traps(Node2D,下面挂陷阱实例)
- 子节点 Camera(预留,等玩家进来)

这是空白关卡框架,后面复制它搭一章4关。

只执行本条指令,做完就停下汇报。
```

**【你应该看到】**: 场景树有上述节点,TileMapLayer是空的(还没画平台)。

**【如果不对】**: 注意Godot 4.7用TileMapLayer不是旧的TileMap,搜节点时搜"TileMapLayer"。

---

#### 步骤8.3:搭建关1(家暴关)

```
【项目锚点】(同上,省略)

复制LevelTemplate另存为 level_ch1_lv1.tscn,按以下布局搭建:
(单位=格,1格=20px;X=距起点,Y=距地面,Y=0为地面)

教学段(X 0~40):
- X=2: 台灯(检查点1,Y=0落地)
- X=14~16: 搓衣板尖刺带(宽3格×高1格,Y=0)
- 平台连续,缺口≤2格(可跳)

核心段(X 40~80):
- X=48: 鸡毛掸子摆锤(锚点Y=8天花板,摆臂4格,±60°,周期2.0s)
- X=60: 酒瓶坠落区(阴影预警)
- 平台有起伏,高低差≤2格

组合考试段(X 80~120):
- X=80: 台灯(检查点2)
- 摆锤+尖刺带组合,间距按《度量速查表》
- 最后一段平坦走廊到关底

关底段(X 120~130):
- X=125: 日记桌道具(prop_diary_desk,Y=0)

背景:ParallaxBackground设置远景/中景图(bg_ch1_lv1_far.png / _mid.png)

只执行本条指令,做完就停下汇报。
```

**【你应该看到】**: 打开场景,能看到平台、台灯、陷阱、日记桌按上面位置摆放。

**【如果不对】**: 
- 如果陷阱素材没到,用ColorRect占位(标注"这是摆锤")
- 如果记不住坐标,每段分开喂:先喂"搭教学段X 0~40",对了再喂"搭核心段"

**重要**: 其他3关(关2下岗/关3渣男/关4育儿)同理,每关一个表格,陈洒一次只搭一关,搭完按F5验证能跑到关底,再搭下一关。表格在附录C。

---

#### 步骤8.4:关底触发日记演出

```
【项目锚点】(同上,省略)

在日记桌道具上挂 Area2D:
- 玩家进入Area2D → GameState切到Cutscene
- 镜头推近笔记本(Camera2D.zoom缓动,约1秒)
- 叠化进日记UI(DiaryUI.show_diary())
- 一章:字迹模糊,音效=笔尖沙沙
- 二章:逐字清晰,音效=纸页摩挲,日期先行

日记结束 → 光片飞入HUD → 翻页转场(PageTurn)→ 下一关

只执行本条指令,做完就停下汇报。
```

**【你应该看到】**: 走到日记桌,镜头拉近,弹出日记UI,关闭后翻页切下一关。

**【如果不对】**: 描述卡在哪步("镜头没拉近"或"UI没弹"),让AI单独修那部分。

---

## H24 里程碑①验收清单

**目标**: 一章四关可玩,从主菜单到关4关底,核心循环跑通。

### 验收项(逐项勾选)

- [ ] **主菜单**: 按任意键能进OP
- [ ] **OP视频**: 播放完自动进关1(或长按1秒跳过)
- [ ] **手感**: 方向键移动流畅,空格跳跃,能跳过3格缺口
- [ ] **陷阱**: 碰到尖刺/摆锤/酒瓶会死
- [ ] **死亡重生**: 碎裂粒子+1秒后台灯旁重生,能继续玩
- [ ] **检查点**: 经过台灯会亮起,死后回最近的灯
- [ ] **日记演出**: 走到关底日记桌,镜头推近,弹日记UI
- [ ] **翻页转场**: 日记关闭后纸页横扫,切到下一关
- [ ] **四关连贯**: 关1→2→3→4 不卡死,能一口气玩到关4关底
- [ ] **LUT调色**: 每关颜色基调不同(家暴=高对比黑,下岗=青灰,渣男=脏红,育儿=冷蓝)
- [ ] **音乐节奏**: M1音乐播放,陷阱周期和节拍对齐(摆锤2秒=4拍)

### 如何验证

1. **清九**: 打开项目,点播放按钮(或F5),从主菜单开始玩
2. **陈洒**: 旁边观察,记录每个问题(卡在哪/缺什么/报错了)
3. **逐项过**: 每项都能正常工作才算通过;有问题立即修,修完再过一遍

### 常见问题处置

| 问题 | 可能原因 | 处置 |
|---|---|---|
| 点播放黑屏无反应 | 主场景没设对 | Project > Project Settings > Application > Run > Main Scene,设为MainMenu.tscn |
| 角色能穿墙 | TileMap碰撞层没设 | 检查TileMapLayer的Physics Layer,确认和角色CharacterBody2D的Collision Mask匹配 |
| 碰陷阱不死 | 陷阱Area2D的Layer和角色Mask不匹配 | 统一约定:角色Layer=1,陷阱Layer=2,角色Mask包含2,陷阱Mask包含1 |
| 死了不重生 | EventBus.player_died信号没连 | 检查控制台有无"player_died"打印,没有=信号没发射 |
| 翻页后卡住 | 场景路径写错 | 检查PageTurn.play_turn()传的路径,确认场景文件存在 |
| 音乐不播放 | Conductor没启动 | 在关卡的_ready()里调Conductor.play(M1音频路径) |
| LUT没效果 | shader没挂对 | 按F12打开调试器,看SubViewport的Material是否有shader |

### 不通过就不准进H24之后

里程碑①是地基,后面全靠它。宁可多花2小时修bug,不要带着破游戏往下走。

---

## H24~36:二章三章开发(清九/陈洒并行)

### 清九任务:二章冲刺与陷阱密度调整(H24~30)

#### 任务9:实现冲刺能力

```
【项目锚点·全部写死不许动】
引擎 Godot 4.7.2(.NET 构建)+ GDScript(不写 C#);写代码前先读 docs/engine-reference/godot/VERSION.md 与附录 A(过时写法黑名单),禁止 Godot 4.6 以前的过时写法。
信号只用 EventBus(autoload)已有的六个,不许新增或改名。
目录:只改【src/player/】,其余只读。
单位:1格=20px(2026-08-29 起,主角20×20基准),数值一律照抄提示词,不许自改。
禁止:重构现有代码、改场景树结构、新增 autoload/插件、用占位冒充完成。

【背景】
第二章玩家获得冲刺能力。破败缺口宽6格(120px),**平跳(4~5格)过不去,必须"起跳→空中按Shift"跳冲组合才能过**(单纯站着冲刺只有3格,比平跳还短;真实跨越上限=跳距+空中冲刺≈7~8格,见D-203公式)。

【任务】
在玩家控制器里添加冲刺:
- 按键:Shift
- 效果:水平瞬发约3格(60px),锁方向0.15秒,然后交还控制
- 空中限一次(落地重置)
- 冷却1秒(避免连续冲刺穿关)
- **冲刺照常判定死亡,不提供任何伤害豁免**(D-537 无无敌帧);冲刺只解决缺口宽度
- **输入缓冲0.1s:玩家离地前几帧按Shift应转为空中冲刺(而非浪费为地面冲刺)**——二章"起跳→空中冲刺"过缺口必需
- Juice:4帧顿帧+残影拖尾+微屏震

二章开场睁眼后播放赠予演出(2秒):
- 女儿脚下亮起冲刺残影特效
- 身体前倾自动演示一小段冲刺位移
- 演出结束,UI弹窗(从上方弹出):"恭喜你学会冲刺 使用shift来拯救过去的'你'吧"
- 弹窗停留2秒自动消失,交还操作

【硬约束】
- 冲刺距离:精确60px(3格)
- 锁方向时间:0.15秒
- 冷却:1.0秒
- 残影:4~6帧,间隔0.05秒,alpha递减
- 顿帧:Time.time_scale=0.1持续4帧(约0.067秒)

【验收】
① 自测:二章开场看到演示冲刺+弹窗;**平跳过不去6格缺口,但"起跳→空中按Shift"能过**(白盒验收:4格缺口平跳稳过、6格缺口只有跳冲能过);连续按Shift有1秒冷却。
② 教我验证:告诉我"按Shift应该看到角色快速前冲约3格(24px),有残影拖尾,画面微卡顿;空中也能冲一次;1秒内只能冲一次。"(过缺口的验证在任务10,靠"起跳→空中冲刺",不是站着冲)
③ 未完成清单:残影/顿帧/屏震哪些是占位。
④ 等我说"通过"。
```

**验收标准**:
- 按Shift,角色快速前冲约3格,有残影
- 空中能冲一次(过6格缺口靠"起跳→空中冲刺",见任务10;站着冲刺过不了)
- 连续按Shift,第二次无效(冷却中)
- 空中冲一次后,再按Shift无效,落地后恢复

---

#### 任务10:二章陷阱密度调整与破败缺口

```
【项目锚点】(同上,省略)
目录:只改【src/traps/, src/levels/】

【背景】
二章复用一章陷阱/地形/角色布局,改三点:①**背景换成 AI 转换生成的二章梦核破败独立素材 `bg_ch2_lvX_far/mid.png`(不再引用 bg_ch1_*,D-535/阻-03)** ②全关挂 lut_ch2 破败灰基调 ③抽掉2~3块平台形成破败缺口。

【任务】
复制一章4个关卡场景,另存为 level_ch2_lv1~4.tscn:
- 保持陷阱位置和类型不变
- **背景素材替换为 bg_ch2_lv1~4_far/mid.png 系列(AI 转换自一章写实背景,已含梦核+破败),不再引用 bg_ch1_* 背景**
- 每关抽掉2~3块平台,形成宽度**6格(48px)**的缺口(必须"起跳→空中冲刺"通过,平跳4~5格不够)
- 缺口位置:教学段1个(安全,摔下无伤可爬回)+ 核心/考试段1~2个(必须跳冲)
- 全关挂LUT:lut_ch2.png(破败灰,只做基调统一;梦核风格化已烘进 bg_ch2 素材,不重复去饱和以免陷阱看不清)

关底获得记忆光片:
- 日记演出结束,1片光从笔记本浮起飞向HUD
- 光片素材:ui_memory_fragment_1~4.png(抽象发光图形)
- 飞行到达,HUD符号从 ◇ 变 ◆,音效"叮"
- 然后翻页转场

二章末关(第4关)特殊:集齐4片后不翻页,触发开锁演出。

【硬约束】
- 缺口宽度:统一6格(48px);通过方式=起跳→空中按Shift(平跳4~5格必掉,单纯站冲3格也必掉)
- 教学段缺口:摔下后能爬回,不强制成功
- 核心段缺口:失败=摔死或撞陷阱
- 记忆光片飞行:Tween,0.8秒,曲线ease_out

【验收】
① 自测:玩二章关1,遇到6格宽缺口,平跳掉下去,站着冲刺也掉,**起跳后在空中按Shift才能过**。
② 教我验证:告诉我"二章的平台有大缺口,平跳过不去、站着冲也过不去,必须先跳起来再空中按Shift冲刺才能过。每关关底光片飞到右上角。"
③ 未完成清单:缺口位置是否按教学/核心分配,末关开锁演出是否占位。
④ 等我说"通过"。
```

**验收标准**:
- 二章关1~4都有明显大缺口
- 平跳过不去,冲刺能过
- 关底光片飞到HUD,符号变实心
- 末关(关4)光片飞完后不翻页(开锁演出占位先打印"集齐四片,应播开锁演出")

---

### 陈洒任务:二章末关开锁演出+三章推门(H24~36)

#### 任务11:二章末关开锁演出(分6小步)

##### 步骤11.1:检测集齐4片

```
【项目锚点】(同上,省略)
目录:只改【src/ui/】

在 DiaryUI.gd 里:
- 记录已获得光片数 collected_fragments: int = 0
- 每次光片飞入HUD,collected_fragments += 1
- 当 collected_fragments == 4 且当前是二章第4关,不调用翻页转场,改调 trigger_unlock_cutscene()

只执行本条指令,做完就停下汇报。
```

**【你应该看到】**: 二章第4关,第4片光飞入后,控制台打印"trigger_unlock_cutscene"。

**【如果不对】**: 检查collected_fragments是否正确计数(打印它看数字)。

---

##### 步骤11.2:短暂静止帧与声音预兆

```
【项目锚点】(同上,省略)

trigger_unlock_cutscene() 执行:
1. 日记UI淡出(0.3秒)
2. 黑屏或画面定格(0.3秒)
3. 播放低频震颤音(音效"笔记本共鸣",0.5秒)

只执行本条指令,做完就停下汇报。
```

**【你应该看到】**: 日记关闭,短暂黑屏,听到"嗡嗡"声。

**【如果不对】**: 音效如果没有,用代码print("播放震颤音")占位。

---

##### 步骤11.3:笔记本特写与4片光飞向锁

```
【项目锚点】(同上,省略)

屏幕中央浮现笔记本特写图(复用主菜单背景裁切或新图):
- TextureRect,居中,从小放大(scale 0.5→1.0,0.5秒)
- 然后4片光从HUD位置依次飞向笔记本锁扣位置
- 每片间隔0.2秒,Tween飞行,带粒子轨迹

只执行本条指令,做完就停下汇报。
```

**【你应该看到】**: 笔记本图放大出现,4个光点依次从右上角飞到笔记本中央。

**【如果不对】**: 检查Tween起点(HUD位置)和终点(笔记本锁扣位置)坐标。

---

##### 步骤11.4:锁扣发光与弹开

```
【项目锚点】(同上,省略)

四光汇聚后:
1. 锁扣发光(粒子爆发,GPUParticles2D)
2. 播放"咔哒"弹开音效(清脆金属声)
3. 可选:显示"锁弹开"状态图(或只音效+粒子)
4. 白光闪(shader从锁扣位置扩散满屏,0.5秒)

只执行本条指令,做完就停下汇报。
```

**【你应该看到】**: 光汇聚,粒子爆发,白光铺满屏幕。

**【如果不对】**: 音效/粒子如果没有,用print占位,白光shader可以先用淡入白色ColorRect代替。

---

##### 步骤11.5:黑屏大字"原来,过去的那个'我'是……"

```
【项目锚点】(同上,省略)

白光后切黑屏,调用 BlackscreenText.show_text():
- 文案:"原来,过去的那个'我'是……"
- 逐字浮现,每字0.1秒
- 显示完停留2秒

只执行本条指令,做完就停下汇报。
```

**【你应该看到】**: 黑屏,白字一个个出现。

**【如果不对】**: 检查BlackscreenText场景是否实例化到UILayer。

---

##### 步骤11.6:黑屏切换"妈妈"+音乐骤停

```
【项目锚点】(同上,省略)

上一屏文字停留2秒后:
1. 切换新黑屏
2. 单独显示"妈妈"二字(字号加倍,48px→96px)
3. 同时 Conductor.stop_music_immediately()(音乐立即静音)
4. 静默4秒(只有呼吸+心跳音,~70BPM渐起)
5. 然后眼睑睁开过场 → 三章

只执行本条指令,做完就停下汇报。
```

**【你应该看到】**: "妈妈"两个大字单独一屏,音乐突然停,4秒后眼睑打开。

**【如果不对】**: 检查Conductor是否有stop_music_immediately()方法,没有就加一个直接AudioStreamPlayer.stop()。

---

#### 任务12:三章相遇(分5小步)

##### 步骤12.1:创建三章场景与背景

```
【项目锚点】(同上,省略)
目录:只改【src/levels/】

创建 level_ch3.tscn:
- 根节点 Node2D
- ParallaxBackground:远景长图 bg_ch3_far.png(宽800~1000,高210,画风渐变交汇;D-534)
- 中景剪影 bg_ch3_mid.png(街边雨棚/电线杆影子)
- 地面平台(TileMapLayer,平坦无陷阱,宽度约100格)
- 门:sprite Door(位置X=50格居中,2态图:关/半开)

只执行本条指令,做完就停下汇报。
```

**【你应该看到】**: 场景里有长背景图(如果素材没到用渐变ColorRect占位)、平坦地面、门。

**【如果不对】**: 手动拖节点,确认TileMapLayer是平的无缺口。

---

##### 步骤12.2:女儿只能向左走

```
【项目锚点】(同上,省略)
目录:只改【src/player/】

三章玩家控制特殊处理:
- 只监听 ← 键(或A键)
- **→ 键:播放"回头看"1帧动画但不移动(阻-09,必做非可选)**——这是三章第一阶段唯一能回答玩家"→键有没有被读到"的反馈,防止"按键不位移"被判成死机
- 无跳跃、无冲刺
- 移动速度固定偏慢(约50px/s,比一章跑速慢30%)
- 播放"慢走"动画(6帧 ~10fps)

在三章场景的脚本里设置玩家 chapter3_mode = true。

只执行本条指令,做完就停下汇报。
```

**【你应该看到】**: 进三章,按←角色播放走路动画(第一阶段位置不动、只有背景滚,见步骤12.4);按→角色播"回头看"1帧但不动;空格/Shift无反应。

**【如果不对】**: 检查玩家脚本是否判断了chapter3_mode,确认输入只接受左键。

---

##### 步骤12.3:母亲NPC同速向右

```
【项目锚点】(同上,省略)
目录:只改【src/levels/】

在三章场景左侧创建年老母亲NPC:
- CharacterBody2D(无操作输入,程序驱动)
- 初始位置:X=10格(屏幕左侧外)
- 移动速度:50px/s向右(与女儿同速)
- 播放"慢走"动画(年老母亲6帧)
- 距离门<3格时,切换到"侧脸回望"帧

只执行本条指令,做完就停下汇报。
```

**【你应该看到】**: 母亲从左边走来,速度和女儿一样,快到门时转头。

**【如果不对】**: 检查母亲velocity是否写死为Vector2(50, 0),确认move_and_slide()在_process里调用。

---

##### 步骤12.4:摄像机固定+背景滚动二段式

```
【项目锚点】(同上,省略)

三章镜头特殊处理:
阶段一(开场20~25秒):
- Camera2D固定位置,居中显示门
- 女儿和母亲**都在画面两侧播放"向中间走"的行走动画,但位置锁定不移动**
- **ParallaxBackground 按输入滚动(阻-09 方案A,关键):只有玩家按住 ← 时背景才滚动,松键立即停(0.3s缓出)**;远景系数0.3,中景0.5——因果反转为"我在推动世界前进",玩家0.5s内即可确认输入有效,避免被判成死机
- 按 → 播放"回头看"1帧但不位移(见步骤12.2)
- 背景滚动到头(长图展示完、画风拼接完整)→ 触发阶段二

阶段二(最后10~15秒):
- 背景停止滚动
- 解锁女儿和母亲的真实位移
- 两人相向而行,在门前相遇(此时按住 ← 真的让女儿向中央位移)

只执行本条指令,做完就停下汇报。
```

**【你应该看到】**: 开场**按住←背景才滚、松手就停**,人物位置不变;背景滚到头后停止,人物开始真的靠近,在门前相遇。

**【如果不对】**: 让AI详细说明"如何让ParallaxBackground随按键滚动(按住←滚、松开停),滚到头后切阶段二",检查是否读了输入而非用Timer自动滚。

---

##### 步骤12.5:推门+白光+ED

```
【项目锚点】(同上,省略)

门前相遇:
- 两人距离<1格,继续按←触发推门
- **母女并肩,同时播放"推门"动画(各1帧,两个sprite靠在一起、手在同一位置=双人并肩推门,中-26)**——玩家按下的这次←同时完成"靠近她"和"推门",是全游戏唯一一次玩家能回应母亲
- 门切换到"半开"状态,旋转约30°
- M3音乐推至最高(B终止式)→ 推门瞬间全静音,切入V1童声清唱《世上只有妈妈好》
- 白光shader从门位置扩散满屏(0.5秒)
- 白光满屏后加载ED视频场景

只执行本条指令,做完就停下汇报。
```

**【你应该看到】**: 门前按←,两人推门,音乐骤停,响起童声,白光铺满,切到ED视频。

**【如果不对】**: 
- 音乐切换如果没素材,print("停M3,播V1")占位
- 白光shader可以先用白色ColorRect淡入代替
- ED视频场景先用黑屏+文字"ED视频占位"

---

## H36 里程碑②验收清单

**目标**: 全流程可穿,主菜单→OP→一章4关→二章4关→开锁演出→三章→ED,不卡死。

### 验收项

- [ ] **一章通关**: 四关连贯,翻页转场正常
- [ ] **二章冲刺**: 能用Shift越过破败缺口
- [ ] **光片收集**: 二章每关关底光片飞入HUD,符号变实心
- [ ] **开锁演出**: 二章第4关,4片光汇聚→锁弹开→黑屏大字"原来…"→"妈妈"→静默4秒
- [ ] **三章相遇**: 只能←走,背景滚动,母亲迎面走来,门前推门
- [ ] **音乐切换**: 推门瞬间音乐停,响起童声(或print占位)
- [ ] **白光转ED**: 推门后白光满屏,切到ED场景(视频或占位黑屏)
- [ ] **无卡死**: 全流程一口气玩下来,不报错不卡死

### 如何验证

从主菜单开始,**不跳过任何演出**,完整玩一遍:
- 记录总用时(目标15~20分钟)
- 记录每次卡顿/报错(截图控制台)
- 记录缺失素材(音效/贴图占位用print/ColorRect的地方)

**测试清单**:
1. 一章每关都要死一次(验证重生)
2. 二章每个缺口都冲刺一次(验证冲刺距离)
3. 开锁演出看完整(验证4片光飞行+黑屏大字)
4. 三章试按→/空格(验证输入锁定)
5. 推门后确认音乐切换(验证M3→V1)

**不通过不准进H36之后。**

---

## H36~44:打磨整合(清九音效接入 / 陈洒 UI 润色 + OP/ED 接入)

### 清九:音效接入(H36~40)
- 把 18 个音效(S1~S18,见美术音乐案)按事件挂载:跳/落地/冲刺/死亡碎裂/**重生(上行单音)**/记忆光片获得/翻页/笔尖沙沙/纸页摩挲/台灯"啪"/**争吵闷响(关1背景)**/机器余音/婴儿啼哭/低频震颤(开锁预兆)/锁"咔哒"/**呼吸+心跳(妈妈黑屏4s)**/三章脚步/主菜单底噪
- **绝不能漏的两条**:争吵闷响(关1施暴者只存于声音)、呼吸+心跳(妈妈黑屏)——D-501 绝不砍项
- 验收:每个事件触发时能听到对应音效;缺的用 print 占位并记入清单

### 陈洒:UI 润色 + OP/ED 视频接入(H36~44)
- 真 UI 素材替换占位:`ui_menu_bg.png`(主视觉)、`ui_diary_open.png`(日记摊开)、`ui_notebook_close.png`、`ui_popup_frame.png`(冲刺窗)、`ui_lock_*.png`、`ui_memory_fragment_1~4.png`
- OP/ED 视频接入:MP4→ogv,挂 VideoStreamPlayer;**ED 备图片序列保底**(D-02);OP 45~60s、ED≈35~40s
- **摆摊保护(中-27)**:主菜单空闲 60s → 自动循环播放路演"揭晓"片段/ED(VideoStreamPlayer+计时器)

---

## H44~48:封版打包

### 打包规格(D-524)
- exe 文件名 `MotherAt20.exe`,窗口标题中文"母亲二十岁"
- 图标:从 `ui_menu_bg.png` 裁"带锁笔记本"制多尺寸 `.ico`(256/64/32/16)
- 导出:Godot Windows Desktop 预设,PCK 内嵌单文件 exe,关闭控制台窗口
- 分发:`MotherAt20_v1.0.zip` = exe + `操作说明卡.png`(方向键/空格/Shift 三行图示)

### 封版前占位清零(专项一第⑤条)
1. 全局搜 `placeholder`,确认 `AssetPaths.gd` 无一行还指向占位
2. 逐关截图对比(纯色方块 vs 有纹理)
3. **陈洒删除 `assets/placeholder/` 整个文件夹 → 跑通全流程**(跑通=零残留,跑不通会立刻报资源加载失败)

### 封版验收
无 Godot 的干净电脑解压双击 → 主菜单一路玩到开发者的话;图标/标题正确、无黑控制台窗口。

---

# 附录 A:Godot 4.7 过时写法黑名单

> ⚠ AI 训练数据多停留在 4.3 前,4.4~4.7 有大量破坏性变更。写码前对照本表,发现 AI 用了左列写法立即纠正为右列。**H0 环境搭建时先过一遍 4.6→4.7 迁移指南**:https://docs.godotengine.org/en/latest/tutorials/migrating/upgrading_to_godot_4.7.html

| 过时/旧写法(禁用) | 4.7 正确写法 | 说明 |
|---|---|---|
| `TileMap` 节点 | **`TileMapLayer`** 节点 | 4.3 起 TileMap 废弃,每层一个 TileMapLayer |
| `move_and_slide(velocity)` 带参 | `velocity=...; move_and_slide()` 无参 | 4.0 起 velocity 是属性 |
| `yield(...)` | `await` | 协程语法 |
| `.connect("sig", self, "func")` | `.sig.connect(func)` | 4.0 信号 Callable 写法 |
| `KinematicBody2D` | `CharacterBody2D` | 4.0 改名 |
| `OS.get_ticks_msec()` 计时游戏逻辑 | `Time.get_ticks_msec()` | OS 部分方法迁到 Time |
| `Reference` | `RefCounted` | 4.0 改名 |
| `export var x` | `@export var x` | 4.0 注解语法 |
| `onready var` | `@onready var` | 同上 |
| shader `hint_albedo` / `SCREEN_TEXTURE` | `source_color` / `hint_screen_texture`+sampler | 4.x shader 变更 |
| `FileAccess` 假设返回值 | 检查 4.4 起 FileAccess 返回类型变更 | 4.4 破坏性变更 |

> **单向平台利好(4.7)**:4.7 原生支持任意方向单向碰撞,单向平台直接用引擎属性,不要让 AI 手写 raycast 方案。

---

# 附录 B:白盒度量速查表(骨架,H8 实测填入)

> 白盒度量关(任务1)跑完后,把实测值填进本表;**此后所有关卡几何只允许引用表内数值**(D-203 铁律)。

| 度量项 | 纸面区间 | 白盒实测值 | 备注 |
|---|---|---|---|
| 平跳最大横距 | 4~5 格 | **5.25 格(105px,20px格体系)** | tools/measure_jump.gd 全速平跳实测;决定尖刺带宽上限(3格=57%余量) |
| 平跳最大净空 | 3~3.5 格 | **3.47 格(69.4px,20px格体系)** | 原地满蓄跳实测;决定台阶高差上限(≤2格) |
| 站立冲刺距 | 3 格(24px) | ____ | 单独用不够过缺口 |
| **跳+空中冲刺跨越上限** | **7~8 格** | ____ | **二章缺口通过依据** |
| 二章破败缺口宽 | 6 格(48px) | ____ | 起跳→空中冲刺,平跳/站冲均过不去 |
| 土狼时间 | 0.15s | ____ | |
| 跳跃输入缓冲 | 0.1~0.15s | ____ | |
| 冲刺输入缓冲 | 0.1s | ____ | 离地前按 Shift 转空中冲刺 |
| 摆锤通行窗 | ≥全速横穿×1.5 | ____ | |
| 冲压机抬起后安全窗 | ≥1.0s | ____ | |

---

# 附录 C:关2/关3/关4 布局表

> 格式照任务8.3 关1 四段式。单位=格,1格=8px;每关约130格,3盏台灯(起点/核心段入口/考试段入口)。陈洒一次搭一关,搭完 F5 验证跑到关底再搭下一关。**背景素材:一章用 `bg_ch1_lvX_far/mid.png`;二章复制后换 `bg_ch2_lvX_far/mid.png`。**

## 关2(下岗关)`level_ch1_lv2.tscn`
- **教学段(X 0~40)**:X=2 台灯①;X=12 零件堆尖刺带(静态,教学跳跃);平台缺口≤2格
- **核心段(X 40~85)**:X=42 台灯②;逆向传送带(带速=跑速50%,段长4~8格,向左拖);高低差≤2格
- **组合考试段(X 85~120)**:X=85 台灯③;传送带+冲压机组合(冲压机周期2.5s,蒸汽预警0.8s)
- **关底段(X 120~130)**:X=125 日记桌 `prop_diary_desk`

## 关3(渣男关)`level_ch1_lv3.tscn`
- **教学段(X 0~40)**:X=2 台灯①;荆棘丛尖刺带(静态);背景远景=暖到腐渐变长图(见 D-512)
- **核心段(X 40~85)**:X=42 台灯②(=中景 a/b 切换点,D-531);腐心平台(踩后1s碎裂3s重生)连跳;发霉巨心(收缩0.8s预警→滴霉液)
- **组合考试段(X 85~120)**:X=85 台灯③;巨心滴液+腐心平台组合
- **关底段(X 120~130)**:X=125 日记桌

## 关4(育儿关)`level_ch1_lv4.tscn`
- **教学段(X 0~40)**:X=2 台灯①;玻璃渣尖刺带(静态)
- **核心段(X 40~70)**:X=42 台灯②;啼哭声波(摇篮摇晃0.8s预警→贴地声波环,跳跃越过)
- **组合考试段(X 70~120)**:X=70 台灯③(=中景 a/b 门框接缝切换点,D-531);玻璃渣+荆棘组合(账单纸风已删,实现难度太高)
- **关底段(X 120~130)**:X=125 日记桌(摇篮里熟睡的孩子暖点)→ 一章末眼睑闭黑屏

> **二章 4 关**:复制上述 + 关1 的场景,另存 `level_ch2_lv1~4.tscn`(倒序:二章第1关=关4育儿素材…第4关=关1家暴素材=老屋);换 `bg_ch2_*` 背景、挂 `lut_ch2`、每关抽2~3块平台成6格缺口。见任务10。

---

# 附录 D:演出状态登记表

> D-532 要求每个演出登记状态。程序据此实现状态切换,防演出-死亡交接 bug。

| 演出 | 状态 | 可跳过 | 允许死亡 | 结束交还 |
|---|---|---|---|---|
| OP 视频 | Cutscene | 是(长按1s) | 否 | 一章开场白光渐暗→Gameplay |
| 日记展示(一章) | Cutscene | 是(按键加速) | 否 | 翻页→Transition→下一关 |
| 日记展示(二章) | Cutscene | 日期不可跳、正文可加速 | 否 | 光片飞入→翻页→下一关 |
| 光片飞入HUD | Cutscene | 否 | 否 | 0.5s后→Gameplay(非末关)/开锁(末关) |
| 开锁演出 | Cutscene | 否 | 否 | 黑屏大字→Cutscene继续 |
| 幕间黑屏大字 | Cutscene | 否 | 否 | 眼睑→Transition→下一章 |
| 三章推门 | Cutscene | 否 | 否 | 白光扩满→ED视频 |
| ED 视频 | Cutscene | 否(不可跳) | 否 | 开发者的话→Menu |
| 翻页转场 | Transition | 否 | 否 | 下一关→Gameplay |
| 眼睑过场 | Transition | 否 | 否 | 下一章→Gameplay |

> 统一死亡流程(D-532):冻结输入→取消演出→停 Conductor 逻辑拍→重置陷阱→重置 UI→回台灯粒子逆聚→**重生后0.5s can_die=false**→恢复 Gameplay。Cutscene 态显式 `can_die=false`。

---

# 附录 E:占位资源全表 + 替换约定(专项一)

> 全部由陈洒生成,美术/音乐不参与。占位放 `assets/placeholder/`,文件名=`placeholder_`+真素材名,尺寸必须一致。

## 占位全表(尺寸对齐 D-509/D-525,含本次审查校正值)
**角色(2)**:`placeholder_char_young_spritesheet.png` 576×24(**24帧**,亮黄#FFD700每帧画"+")→`char_young_spritesheet.png`;`placeholder_char_old_mother.png` 192×24(8帧,灰褐#8B7355)→`char_old_mother.png`。⚠ 24帧不是31帧。

**平台地形(3)**:`placeholder_tile_platform.png` 8×8 亮绿#4CAF50;`placeholder_tile_crack.png` 16×16 半透明白画裂线;`placeholder_platform_rotten.png` 16×8 粉#E91E63。

**陷阱(危险物红#F44336,不致死物橙#FF9800)**:trap_pendulum 40×120 / washboard 60×20(可旋转) / conveyor 200×12(橙,薄带,可旋转) / part 60×20(可旋转) / press 40×40 / thorns 60×20(可旋转) / heart_big 60×60 / droplet 20×20 / bottle 20×20 / soundwave 60×40(隐藏→出现→扩大) / glass 60×20(可旋转) / rotten 40×20(橙),均 `placeholder_trap_*.png`。(billwind 账单纸风已删;尖刺带类全部支持R键90°旋转)

**道具交互(黄#FFC107)**:lamp 8×16 / diary_desk 48×32 / cradle 16×16 / door 24×32(2态),`placeholder_prop_*.png`。

**背景(12,纯色,高210)**:一章 lv1/lv2 各 far/mid 640×210;lv3/lv4 far 1280×210 横向渐变(lv3粉→暗红,lv4暖黄→冷蓝)、mid_a/mid_b 640×210;三章 far 1000×210 灰→彩虹、mid 640×210。`placeholder_bg_*.png`。二章不单独做,复用一章占位,等 AI 转出 `bg_ch2_*` 后改路径。

**UI(9,统一1920×1080)**:menu_bg / diary_open / notebook_close / lock_closed / lock_open / memory_fragment_1~4(各64×64)/ popup_frame / eyelid / page_turn,`placeholder_ui_*.png`。眼睑/翻页程序已用 ColorRect/Tween 实现,可跳过图片占位。

**LUT(1)**:`placeholder_lut_neutral.png` 256×16 中性(画面无变化),6个关卡先都挂它;另做 `placeholder_lut_test_invert.png`(反色)专供验证 shader 是否生效。

**音频**:`placeholder_M1/M2.ogg` 必须是**真120BPM节拍器**(不能静音,否则 Conductor 无法验收);M3/V1 静音占位;12音效共用一个0.2s正弦"哔"。

## 替换约定
1. **默认同名替换不改代码**:建 `AssetPaths.gd`(autoload),每个资源一个路径常量;真素材到位只改这一个文件的一行,场景/代码不动,改完F5确认无白块。用常量而非覆盖文件=Git留痕+可回退。
2. **唯一例外——二章背景改路径不改名**:AI 转出 `bg_ch2_*` 后在 `AssetPaths.gd` 新增 `BG_CH2_*` 常量,把二章4关引用从 `BG_CH1_*` 换过去。这8处不会因文件名对不上报错,漏了就是二章跟一章一样、反转失效,必须单独打勾。
3. **台账**:`production/session-state/asset-replacement.md`(资源名/占位或真素材在用/替换时间),**陈洒维护**;清九拿到新素材口头通知陈洒改常量,避免两人同改一文件。
4. **删占位**:H44 由陈洒一次性删 `assets/placeholder/` 整文件夹再跑通(跑通=零残留)。
5. **三道清零关**:里程碑①②验收各加"占位清零检查"+ H36逐关截图 + H44删文件夹跑通。
6. **素材不合用**:退回占位重新生成,不要改代码适配单张素材(会破坏8px网格对齐)。
