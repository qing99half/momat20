# H24 前问题判定 + 修复方案 + H24 后策划更新(20px 体系)

> 2026-08-29 评审。**本文档只出方案,不动代码。**
> 依据:`剧情.md`、`策划案-程序.md`、`策划案-美术音乐.md` + 当前代码实测(src/、levels/、assets/placeholder/)。

---

## 〇、当前像素体系权威值(后文所有更新的基准)

从代码与占位素材实测,现行体系为 **1格 = 20px**(2026-08-29 起,主角 20×20 基准):

| 项目 | 权威值 | 出处 |
|---|---|---|
| 格子 | 1格 = 20px | `ModuleRegistry.CELL = 20.0` |
| 游戏层 | 640×360(32×18格=整关高) | `MainGame.gd` `_fit_game_layer()` |
| 地面行 / 关卡顶 | Y=320px / Y=0 | `LevelSpecs.gd` |
| 关卡长 | 130格 = 2600px(格数不变) | `LevelSpecs.gd` |
| 角色精灵 | 20px/帧:年轻体 480×20(24帧)、母亲 160×20(8帧) | 占位素材实测 |
| 平台 tile | 20×20 | `placeholder_tile_platform.png` |
| 道具 | 台灯 20×40 / 日记桌 120×80 / 摇篮 40×40 / 门 60×80(2态) | 占位素材实测 |
| 背景 | **高一律 360**;宽:lv1 far 640 / 其余关 far 800~1280 / mid 800 / 三章 far 2000、mid 1000 | 占位素材实测 |
| 陷阱 | 摆锤 40×120、尖刺带类 60×20、传送带 200×12、冲压机 40×40、巨心 60×60、酒瓶/滴液 20×20、声波 60×40、腐板 40×20 | 占位素材实测 |
| UI 层 | 1920×1080(窗口空间,**不随像素体系变**);记忆光片 64×64 | 占位素材实测 |
| 整数倍缩放 | 720p×2 / 1080p×3 / 2K×4 | `MainGame.gd` 注释 |

> ⚠ `像素十倍化清单.md`(8px→80px、3200×1800)是**另一套未执行的方案**,与现行 20px 体系无关。建议在文件头标注"已作废,以 20px 体系为准"后归档,避免 AI 操作员拿错数值。

---

## 一、三个疑似问题的判定

### 问题1:第一章应是"写入日记"而不是"打开日记" —— ✅ 真实偏差(叙事级 bug,必须修)

**设计原文**:
- `剧情.md`:一章每关通关后"日记展开,**写下**第一段";日记要**到第二章才能读**,日期(1993/1996/1997/1999)是全游戏唯一的反转证据。
- `策划案-程序.md` 步骤8.4:一章=字迹模糊+笔尖沙沙;二章=逐字清晰+纸页摩挲+**日期先行**。
- `策划案-美术音乐.md`:笔记本定妆要求"另出同一段字迹的**模糊版**,供一章'字迹模糊版'日记底图"。

**代码现状**(三处全部走样):
1. `DiaryUI.gd` `show_diary()`:**一章也把日期清晰逐字打出**(只有正文 alpha=0.45 模糊)。而 `DiaryDesk.gd` 默认 `diary_date="1993年6月12日"`、`chapter=1` —— 第一章就把"1993年"甩在玩家脸上,**反转证据提前一整章泄底**。
2. `DiaryDesk.gd` 默认正文是可读句子("他又摔了酒瓶……妈妈带你离开这里"),一章呈现的是"打开一本可读日记",不是"写入"。
3. `HUD.gd` 无条件监听 `diary_finished` → **一章每关也飞记忆光片、点亮 ◆**。记忆光片是二章专属收集物(美术案:光片语境是"每读完一篇日记";程序案任务10:光片=二章关底奖励)。一章飞光片既是机制穿帮,还会污染二章计数(任务11 的 `collected_fragments==4` 判定会被一章的 4 枚提前撑满)。

**修复方案**:
1. `DiaryUI.show_diary()` 分两种模式:
   - **写入模式(chapter==1)**:DateLabel 整段隐藏(一个像素都不显示);正文区用美术的"模糊手写字迹底图"逐行浮现(不用可读文本);音效=笔尖沙沙;无跳过提示(整段约 3~5s 自动播完);结束发 `diary_finished`。
   - **读取模式(chapter>=2)**:维持现状——日期先行逐字、日期打完前禁跳(D-505)、正文可加速、纸页摩挲音效。
2. 一章的 `DiaryDesk` 不再传真实日期/正文(`diary_date`/`diary_text` 留空或传伪字迹占位),真实 1993/1996/1997/1999 四篇文案只配在二章四关。
3. `HUD` 增加章节门控:`GameState.current_chapter >= 2` 才显示 HUD、才响应 `diary_finished` 飞光片;一章 HUD 整体隐藏。
4. `策划案-程序.md` 步骤8.4 措辞从"弹日记UI"改为"一章=写入演出(模糊字迹浮现,不显示日期),二章=读取演出(日期先行)";任务7.2 的 D-505 日期规则注明**仅二章适用**。

---

### 问题2:一章关内通关后切换到下一关 —— ✅ 真实 bug(链路是半成品,目前必然"留在本关")

**代码现状**(链路每一环都在,但没接通):
1. `DiaryDesk._play_cutscene()` 的演出链路完整:镜头推近 → 日记 → 等 HUD → 发 `level_completed` → `PageTurn.play_turn(next_scene_path)`。**但 `next_scene_path` 为空时走"留在本关"分支。**
2. `levels/ch1_lv1.json` 里 diary_desk 模块 `params={}` → `next_scene_path=""` → 现状必走"留在本关"。
3. `ModuleRegistry.instantiate()` **只给 PlatformModule 传 params**,DiaryDesk 的导出参数(level_id/日期/正文/next_scene_path)从 JSON 根本传不进去 —— 就算 JSON 里写了 params 也无效。
4. 全项目只有 lv1 一关(`src/levels/` 和 `levels/` 都只有 ch1_lv1),lv2~4 还没搭。
5. 最要命的一条:`MainGame.gd` 用常量 `DEFAULT_LEVEL_JSON` **硬编码加载 ch1_lv1.json** —— 就算翻页切场景切到 MainGame,也永远回到第 1 关。
6. `EventBus.level_completed` 发射后**全项目无任何监听者**(已全局 grep 确认)。

**修复方案(推荐方案 A,改动最小)**:
1. `GameState`(autoload,切场景不销毁)增加关卡链:
   - `LEVEL_CHAIN = ["ch1_lv1","ch1_lv2","ch1_lv3","ch1_lv4","ch2_lv1",...,"ch2_lv4","ch3"]`
   - `current_level_index: int`、`current_chapter: int`
   - `advance_level()`:index+1、按 index 推算 chapter、返回是否触发章级过场。
2. `MainGame._ready()` 改为按 `GameState` 当前 index 拼 JSON 路径(`res://levels/%s.json`),删掉 `DEFAULT_LEVEL_JSON` 常量;编辑器试玩路径优先级不变。
3. `DiaryDesk` 演出结束后:调 `GameState.advance_level()` → `PageTurn.play_turn("res://src/MainGame.tscn")`(目标恒定,**每关不再需要配 next_scene_path**,该导出参数删除)。
4. `ModuleRegistry` 给 diary_desk 传 params(level_id/chapter/二章的日期与正文),让地图编辑器摆关底桌时可配文案。
5. lv2~4 关卡 JSON 用地图编辑器按附录C 四段式搭建(这本来就是 H16~24 任务8 的收尾量)。

**方案 B(备选,不切场景)**:MainGame 监听 `level_completed`,翻页遮满期间 free 旧关卡、`LevelLoader.build()` 下一关塞进 SubViewport。省一次场景重建、HUD/日记UI 实例保持,但要手动重置相机/Conductor/HUD 状态,容易漏。两人分工下推荐方案 A(重建场景=状态天然清零,不会留脏状态)。

---

### 问题3:一章通关后跳转二章 —— ✅ 真实(不是 bug,是完全未开发;且"逻辑没理清"的判断成立)

**代码现状**:
- 代码里**没有"章"的概念**:`GameState` 无 chapter 字段;`EventBus.dash_unlocked`、`door_opened` 两个信号**全项目零消费**(已 grep 确认)。
- 一章末"镜头缓缓闭眼,黑屏"(剧情.md)、二章开场"黑屏睁眼+'我一定要拯救过去的我'黑屏大字"均无实现;`EyelidTransition`、`BlackscreenText` 组件已存在但没有编排者。
- `levels/` 无二章关卡;无二章 LUT 切换;无 `bg_ch2_*` 路径切换(附录E 替换约定第2条的 8 处改路径无人执行)。

**修复方案(把章级流程定义死)**:
1. **转场分工**(附录D 演出状态表本来就写对了,照它实现):
   - **关级转场 = 翻页**(PageTurn):lv1→2→3→4。
   - **章级转场 = 眼睑过场**(EyelidTransition):一章→二章、二章→三章。
2. 一章末(ch1_lv4 日记写入演出后)**不翻页**,走:
   `diary_finished → 眼睑 close_eyes() → 黑屏 → BlackscreenText.show_text("我一定要拯救过去的'我'。") → GameState 切 chapter=2、advance 到 ch2_lv1 → EventBus.dash_unlocked.emit() → 眼睑 open_eyes() → 二章开场冲刺赠予演出(任务9 的演示+弹窗)`
3. `Player` 监听 `dash_unlocked` 解锁冲刺(消费掉这个信号);`current_chapter>=2` 时 HUD 才显示(与问题1修复共用章节门控)。
4. 二章 4 关 = 复制一章 JSON 倒序改名 + 抽平台成 6格缺口 + 换 `bg_ch2_*` 路径 + 挂 `lut_ch2`(H24~36 正常开发量,不算 bug)。

**顺带发现的相关隐患(一并修,不然 H36 验收必踩)**:
- 一章 HUD 就亮着 `◇◇◇◇`(应在二章睁眼时才出现)。
- `DiaryDesk` 演出 await 链没有死亡防护:附录D 要求 Cutscene 态 `can_die=false`,需确认 Player 在 frozen/Cutscene 下不吃陷阱 body_entered(否则"关底进演出的同帧碰陷阱"会死在日记里)。
- 任务11 的 `collected_fragments` 计数应放在 GameState(切场景不丢),不能放 DiaryUI(方案 A 每关重建场景,放 UI 里会被清零)。

---

## 二、H24 之后策划内容按 20px 体系更新(逐条)

> 换算总则:**格数、时间/时长类(周期/预警/土狼/缓冲/演出秒数)一律不变;px = 格数 × 20;UI 层(1920×1080)不动。**

### 策划案-程序.md

| 位置 | 现文(8px 时代残留) | 改为(20px 体系) |
|---|---|---|
| 任务9 验收② | "快速前冲约3格(**24px**)" | "快速前冲约3格(**60px**)" |
| 任务10 正文+硬约束 | "宽度**6格(48px)**" ×2处 | "宽度**6格(120px)**" |
| 任务10 正文 | "平跳4~5格不够" | "平跳实测5.25格=105px 不够"(附录B已有实测) |
| 任务10 背景素材 | 未给尺寸 | 补:bg_ch2 与一章对应关**同尺寸**(高360;lv1 far 640 / lv2 far 800 / lv3、lv4 far 1280 / mid 均 800),AI 转换时按此出图 |
| 步骤12.1 | "bg_ch3_far.png(宽800~1000,**高210**)" | "bg_ch3_far.png(**宽2000,高360**;占位已按 2000×360 制作)" |
| 步骤12.1 | "中景剪影 bg_ch3_mid.png" 无尺寸 | 补:1000×360 |
| 步骤12.1 | "地面…宽度约100格" | 不变(100格=2000px,与 far 图同宽,格数本来就对) |
| 步骤12.1 | "门:sprite Door" 无尺寸 | 补:60×80,2态(door_closed / door_half_open) |
| 步骤12.2/12.3 | "移动速度固定偏慢(约50px/s,比一章跑速慢30%)" | 保留 50px/s 数值但**重标含义**:20px 体系下=2.5格/s(三章是演出关,时长 D-534=30~40s 不变,速度按观感在白盒验收时微调,不调格数);"慢30%"一句删掉(8px 时代跑速 90px/s 的相对值,现已无意义) |
| 步骤12.3 | "初始位置 X=10格""距离门<3格" | 格数不变;补 px 注解:200px / 60px |
| 步骤12.5 | "两人距离<1格" | 不变(=20px) |
| 附录B | "站立冲刺距 3格(**24px**)" | "3格(**60px**)" |
| 附录B | "跳+空中冲刺跨越上限 7~8格" | 补 px:**140~160px** |
| 附录B | "二章破败缺口宽 6格(**48px**)" | "6格(**120px**)" |
| 附录C 头 | "单位=格,**1格=8px**" | "单位=格,**1格=20px**"(格坐标全部不变) |
| 附录E 占位全表 | char 576×24、lamp 8×16、diary_desk 48×32、door 24×32、tile 8×8、bg 640×210 等 | 整表替换为本文档「〇、权威值」一表(占位素材已按新尺寸生成,附录E 落后于现实) |
| 附录E 末尾 | "会破坏**8px**网格对齐" | "会破坏**20px**网格对齐" |
| H24 里程碑①清单 | — | 追加3项验收:**①一章日记不显示日期、不飞光片;②关底翻页能进下一关(非"留在本关");③lv4 关底走眼睑闭眼+黑屏大字进二章** |
| H36 里程碑②清单 | "一章通关:四关连贯,翻页转场正常" | 细化:"四关连贯,**lv1→2→3→4 每关都真实切换**(控制台打印关卡 id 递增),lv4 后**眼睑过场**进二章而非翻页" |

**新增任务10.5(流程补链,就是问题2/3 的修复落地,建议插进陈洒 H24~30)**:
> 任务10.5:关卡链与章节过场
> 1. GameState 加 LEVEL_CHAIN / current_level_index / current_chapter / advance_level()
> 2. MainGame 按 GameState 读关;DiaryDesk 关底调 advance_level() 后翻页恒切 MainGame.tscn
> 3. 章末(lv4/lv8)改走眼睑+黑屏大字,不进翻页
> 4. 验收:从 lv1 一口气玩到 ch2_lv1,控制台关卡 id 依次递增,一章末看到"我一定要拯救过去的'我'。"

### 策划案-美术音乐.md(H24 后涉及部分)

| 位置 | 现文 | 改为 |
|---|---|---|
| 角色 sprite sheet | 年轻体 576×24(24帧×24px) | **480×20(24帧×20px)** |
| 角色 sprite sheet | 母亲 192×24(8帧×24px) | **160×20(8帧×20px)** |
| 背景需求表(五关) | 640×210 / ≥1000×210 | 高一律 **360**;宽:lv1 far 640、lv2 far 800、lv3/lv4 far 1280、三章 far 2000;mid 一律 800(三章 mid 1000) |
| 陷阱画布(D-509 权威值) | 摆锤16×48 / 搓衣板24×8 / 传送带64×8 / 零件24×8 / 冲压16×8 / 腐板16×8(3态) / 滴液8×8 / 酒瓶8×8 / 声波24×16 / 台灯8×16 等 | 全部 ×2.5:摆锤40×120 / 搓衣板60×20 / 传送带200×12(程序已按此宽度做占位) / 零件60×20 / 冲压40×40 / 腐板40×20(3态) / 滴液20×20 / 酒瓶20×20 / 声波60×40 / 台灯20×40 |
| 道具 | 日记桌48×32 / 门24×32×2态 / 摇篮16×16 | 日记桌**120×80** / 门**60×80**×2态 / 摇篮**40×40** |
| UI 章节 | "窗口模式(1280×720,320×180整4倍)" | "1280×720 = 游戏层 640×360 整2倍;1080p 整3倍";UI 素材 1920×1080 不变 |
| 记忆光片 | 64×64 | 不变(HUD 在窗口空间) |
| 二章 bg_ch2_* | 无尺寸 | 与一章对应关同尺寸(见上),AI 转换出图按 360 高 |

### 剧情.md

无数值,**不动**。但建议在顶部失效条目区追加第 8 条:"一章日记=**写入演出**(模糊字迹、不显示日期);'日记展开写下第一段'不是打开可读日记"——防止后续操作员照"日记展开"字面再做成打开式。

---

## 三、修复优先级建议(进 H24 前必须先做)

1. **P0(挡反转,立刻修)**:问题1 —— 一章不显示日期、不飞光片、HUD 章节门控。不修=游戏核心反转报废。
2. **P0(挡流程)**:问题2 —— GameState 关卡链 + ModuleRegistry 传参 + lv2~4 搭建。不修=里程碑①"四关连贯"永远勾不上。
3. **P1(H24 开工即做)**:问题3 —— 章级过场编排(任务10.5)。组件都在,只缺编排,约半天。
4. **P1(文档同步)**:本文档第二节的策划案数值更新,1 小时内人工核对改完,改完再发 H24 任务提示词(提示词里数值照抄策划案,策划案错了 AI 就写错)。
