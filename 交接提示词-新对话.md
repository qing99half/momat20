# momat20 交接提示词（新对话用）

> 用法：把本文件全文粘贴给新对话作为第一条消息，然后说"阅读完开始工作"。
> 生成时间：2026-08-30 04:00 · 代码最新提交 `90305d0`（已推送 GitHub main）

---

## 0. 你接手的项目

Godot 4.7.2 像素平台跳跃游戏《momat20》（母女双章叙事：一章女儿视角 4 关 → 二章"拯救"倒序 4 关 → 三章相向奔赴）。
**关卡由用户（陈洒）在自研地图编辑器里亲手摆放，AI 绝不替用户生成/修改关卡内容。**

## 1. 环境铁律（违反必翻车）

- 工作区：`D:\8.26黑客松\program\momat20`，所有产出只放这里
- Godot：`D:\Godot_v4.7.2-stable_mono_win64\Godot_v4.7.2-stable_mono_win64_console.exe`（headless 冒烟用 console 版；起编辑器走 bat）
- GitHub 推送必须带代理：`git -c http.proxy=http://127.0.0.1:7890 push origin main`
- 重启编辑器：`taskkill //F //IM Godot_v4.7.2-stable_mono_win64.exe` → `cmd //c start "" "D:\8.26黑客松\program\momat20\地图编辑器.bat"` → tasklist 确认存活
- **无头冒烟三大坑**：①`--script` 入口不得静态引用 autoload 相关全局类（用 `load()` 动态加载）；②headless 下截图必崩（dummy rendering），截图只能窗口模式；③headless 帧速失真，等待用 `create_timer()` 或 `await physics_frame`
- 测试脚本必须设 `get_tree().current_scene = mg`，否则切场景不释放旧场景→双玩家假 bug
- tscn 文件不支持 `#` 注释
- Edit 文件前必须重读（可能有其他进程改过）；`git status` 里陌生人的产物默认不碰

## 2. 当前体系快照

- 20px 格；640×360 离屏渲染（SubViewport）+ GameView 精灵小数缩放铺满窗口；F11 全屏；相机只横移
- 关卡 = `levels/*.json`（`{level_id, bg_far, modules:[{id,px,py,params}]}`），LevelLoader 构建；试玩/正式链共用 MainGame.tscn
- 模块换皮寻址：`assets/art/<level_id>/`，回退链 `本关 → ch1同序号(ch2限定) → ch1_lv1~lv4 → 其他ch2关 → 占位色块`（2026-08-30 上线，冒烟 PASS：`tools/smoke_skin_chain.gd`）
- 中景层已全部删除（只留远景视差，调暗+模糊 shader）；编辑器存档不再写 bg_mid
- 键位：移动 方向键/A/D；跳 W/空格；**冲刺 F / Shift / 鼠标右键**（右键=中文输入法保底，IME 永不拦截右键；字母键和 Shift 在中文输入法下可能被吃）
- 冲刺参数：60px/0.15s/400px/s/冷却1s/空中限一次/帧21；开局即解锁（dash_unlocked 前置）
- 章节 BGM：ch1→bgm_m1 / ch2→bgm_m2 / ch3→bgm_m3_a；Conductor 120BPM 对拍，按音频时长自动循环
- 编辑器：`地图编辑器.bat`；正式链：`开始游戏.bat`（ch1_lv1 起跑）

## 3. 用户关卡存档现状

- ch1_lv1~lv4：86/82/128/84 个模块，已验收
- ch2_lv1：41 个模块 —— **但用户摆的是一章 lv1 的搓衣板/摆锤/酒瓶，与二章倒序设计冲突（见 P0-2）**
- ch2_lv2~lv4：尚未搭建
- 一章→二章关卡链端到端已验证可通关（lv1→lv2→lv3→lv4→二章章级过场）

## 4. 待解决问题（按优先级）

### P0-1 冲刺实机验证（刚修完，等用户确认）
- 根因：赠予弹窗文案写"使用shift"，用户按 Shift 被中文输入法吃掉；字母 F 在中文输入法下也可能被吃
- 已修（`90305d0`）：冲刺支持 F / Shift / 鼠标右键三键位，弹窗文案同步更正
- **新对话第一件事：问用户右键冲刺是否正常**；若仍不行，查 `_input` 是否收到事件（加 print 实测）

### P0-2 二章顺序反向（用户定性 P0，需重读策划案后给方案）
- 依据：`剧情.md` 第 3 行"二章为**倒序 4→3→2→1**（D-105）"、第 35 行"**从右向左闯关**"；`策划案-程序.md` 第 1411 行"二章第1关=关4育儿素材…第4关=关1家暴素材=老屋"、第 961 行"二章复用一章布局抽 2~3 块平台成 6 格破败缺口，必须跳冲组合过"
- 现状：素材映射已是反向（ch2_lv1=育儿关素材 glass/soundwave/billwind；ch2_lv4=家暴关素材 bottle/pendulum/washboard）✓
- 缺口：①用户在 ch2_lv1 摆了家暴关陷阱（搓衣板×25），与倒序设计冲突——需与用户确认是重建 ch2_lv1（改用育儿关素材）还是用户有意为之；②**从右向左闯关完全未实现**（现全部左→右），这是大改：出生点在右、关底在左、相机 limit 方向、陷阱朝向；③日记日期 1993/1996/1997/1999 与倒序关的对应关系要在策划案里核准（`策划案-程序.md` 第 29 行）
- 先出方案给用户确认，再动代码

### P1-1 试玩路径缺演出（用户问"是试玩导致的吗"——是）
- 星星 HUD（`src/ui/HUD.gd`，◆◇ 光片计数）、冲刺赠予演出（`MainGame._play_dash_gift_after_intro`）、提示弹窗都挂在 GameState 章节流转上：赠予演出要求 `chapter_intro_pending==true && current_chapter==2`，提示弹窗只在 `chapter==1 && level_index==0` 播
- 编辑器试玩走 `GameState.sync_chapter_from_level_id`（MainGame.gd:26），不过章级过场，所以这些都不触发
- 修复方向：试玩 ch2 首关时模拟赠予演出（编辑器试玩入口置标志或直接调 `play_dash_gift_cutscene`）；星星 HUD 门控逻辑先读 `src/ui/HUD.gd` 与 `GameState` 再定；**用户已同意：整体测试时再统一做**
- 光片真素材 `ui_memory_fragment_1~4.png` 仍是占位（HUD.gd:18-21）

### P2 挂起项
- 声波陷阱新素材（空心断续圆环，提示词已给用户）到位后替换进 `soundwave` 的 TrapConfig
- ch2_lv2~lv4 等用户搭建；二章日记桌可配 `diary_date`/`diary_text` params（ModuleRegistry 已支持）

## 5. 汇报风格

- 用户是策划不是程序：汇报给结论+实测数值，少贴代码；改代码前先给方案等确认（用户明确说"直接做/动代码"除外）
- 用户会情绪上头，稳住，用实测数据说话
