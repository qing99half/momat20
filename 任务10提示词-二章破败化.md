# 任务10 提示词包:二章破败化(20px 体系·按当前代码现状整理)

> 2026-08-29 整理。**只整理提示词,不写代码。**执行时再逐条粘贴给 Claude Code。
> 与旧版任务10 的差异:①数值全部按 1格=20px(缺口6格=**120px**,站冲3格=**60px**);②关卡载体已从 .tscn 场景变为 **levels/*.json + 地图编辑器**;③大量"前置"已被并行施工完成,提示词里明确列出,防止 AI 重做。

---

## 〇、开工前现状快照(已核实,勿重做)

已完成(存在即事实,AI 不得重复实现):

- **冲刺(任务9)**:`Player.gd` 已实现。60px/锁方向0.15s/冷却1s/空中限一次/缓冲0.1s/无伤害豁免;`dash_unlocked` 由 `GameState.current_chapter>=2` 自动置位 + `EventBus.dash_unlocked` 兜底。
- **关卡链(任务10.5)**:`GameState.LEVEL_CHAIN = [ch1_lv1..ch1_lv4, ch2_lv1..ch2_lv4, ch3]`;`advance_level()` 返回 "level"/"chapter"/"end";关级=翻页、章级=眼睑过场,`DiaryDesk` 已接线;一章末黑屏大字"我一定要拯救过去的'我'。"已配置。
- **赠予演出**:`MainGame` 在二章首关睁眼后自动调用 `play_dash_gift_cutscene()`(任务9 已实现)。
- **光片计数**:`GameState.add_fragment()` 已计数;集齐4片且在 ch2_lv4 → `unlock_pending=true`,`DiaryDesk` 不翻页(任务11 开锁演出接管点已留好)。
- **LUT 后处理**:`src/ui/LutPostProcess.gd` 存在,默认挂中性 LUT;**但 JSON 关卡暂不支持按关指定 LUT**(本包提示词 A 补上)。
- **关卡载体**:`levels/ch1_lvX.json`(bg_far/bg_mid/level_id/modules),用地图编辑器(`地图编辑器.bat`)搭建;`LevelLoader` 按 `GameState.current_level_id()` 拼路径加载。

当前缺口(=任务10 真正要做的):

- `levels/ch2_lv1~4.json` 不存在 → 本包提示词 B。
- JSON 不支持 `lut` 键 → 提示词 A(小改 LevelLoader)。
- `assets/placeholder/placeholder_bg_ch2_*.png`、`placeholder_lut_ch2.png` 不存在 → 提示词 C。

---

## 提示词 A:JSON 关卡支持 `lut` 键(清九,约 15 分钟,先做)

```
【项目锚点·全部写死不许动】
引擎 Godot 4.7.2(.NET 构建)+ GDScript(不写 C#);写代码前先读 docs/engine-reference/godot/VERSION.md 与附录 A(过时写法黑名单),禁止 Godot 4.6 以前的过时写法。
信号只用 EventBus(autoload)已有的六个:player_died / level_completed(id) / beat(n) / dash_unlocked / diary_finished / door_opened,不许新增或改名。
目录:只改【src/levels/, src/ui/LutPostProcess.gd】,其余只读。
单位:1格=20px(主角20×20基准),数值一律照抄提示词,不许自改。
禁止:重构现有代码、改场景树结构、新增 autoload/插件、用占位冒充完成。

【背景】
LutPostProcess.gd 已实现 LUT 调色(export lut_texture,默认中性)。
但 levels/*.json 关卡数据没有 lut 字段,二章需要全关挂破败灰 LUT(lut_ch2)。
关卡链/换关已由 GameState+MainGame 实现,不要碰。

【任务】
1. LevelLoader.build():读 JSON 可选键 "lut"(值为贴图路径,空=中性LUT),
   把值存到关卡根的 meta("lut_path")。
2. MainGame 已有 LutPostProcess 节点(在 MainGame.tscn 里,先读场景确认节点名/组),
   在 MainGame._ready 装关后:若关卡 meta 有 lut_path 且资源存在,赋给 lut_texture;否则保持中性。
   (MainGame.gd 只许加这几行,不准动现有关卡链/赠予演出逻辑。)
3. 若 LutPostProcess 不在场景里而是别的方式挂载,先汇报现状再动手,禁止自创第二套后处理。

【硬约束】
- lut 键可选:没有该键的关(ch1)行为与现在完全一致
- 资源不存在时 push_warning 并回退中性 LUT,不报错不崩

【验收】
① 自测:给 ch1_lv1.json 临时加 "lut": "res://assets/placeholder/placeholder_lut_test_invert.png",
  F5 画面反色;删掉该行恢复中性。验证完把 ch1_lv1.json 恢复原样。
② 教我验证:告诉我"我在关卡JSON里加一行lut就反色,删掉就恢复"。
③ 未完成清单:如实列。④ 等我说"通过"。
```

---

## 提示词 B:搭建二章 4 关(陈洒,核心工作量)

> 前置:提示词 A 已通过;`levels/ch1_lv1~4.json` 已全部搭完(目前只有 lv1/lv2,先补完一章再复制)。

```
【项目锚点·全部写死不许动】
引擎 Godot 4.7.2(.NET 构建)+ GDScript(不写 C#);写代码前先读 docs/engine-reference/godot/VERSION.md 与附录 A(过时写法黑名单)。
信号只用 EventBus(autoload)已有的六个,不许新增或改名。
目录:只改【levels/】,其余只读(地图编辑器可用,不改编辑器代码)。
单位:1格=20px(主角20×20基准),数值一律照抄提示词,不许自改。
禁止:用占位冒充完成、新增 autoload/插件。

【背景】
二章=一章四关的"破败残破版+从右向左重来"(倒序,D-105):
  ch2_lv1 = 一章关4(育儿)布局   ch2_lv2 = 一章关3(渣男)布局
  ch2_lv3 = 一章关2(下岗)布局   ch2_lv4 = 一章关1(家暴)布局=老屋
玩家已有冲刺(60px/0.15s/冷却1s/空中限一次),破败缺口必须用它过。
关卡链、翻页、章级过场、光片计数、末关 unlock_pending 全部已实现,你只产 JSON 数据。

【任务】(一关一关来,每关做完 F5 验证再做下一关)
1. 复制 levels/ch1_lv4.json → levels/ch2_lv1.json(以此类推,倒序映射见上)。
2. 每个 ch2 JSON 改四处:
   ① "level_id": 改为 "ch2_lvX"
   ② "bg_far"/"bg_mid": 改为 res://assets/placeholder/placeholder_bg_ch2_lvX_far.png / _mid.png
      (lv3/lv4 的中景是 mid_a/mid_b 两张的关,照一章结构保留两张,文件名同步换 ch2)
   ③ 加一行 "lut": "res://assets/placeholder/placeholder_lut_ch2.png"
   ④ 关底 diary_desk 模块 params 配二章日记(读取模式):
      chapter=2,diary_date 按下表,diary_text 用该关一章文案(若未定,先占位但日期必须照表):
        ch2_lv1(育儿)= 1999年  ch2_lv2(渣男)= 1997年
        ch2_lv3(下岗)= 1996年  ch2_lv4(家暴)= 1993年
      (四个日期是反转唯一证据,错一个=反转报废;ModuleRegistry 已支持 diary_desk 传参,先核实,不支持就停下手汇报)
3. 抽平台形成破败缺口,每关 2~3 处:
   - 缺口宽度:统一 6格=120px(在编辑器里删/挪 ground、platform 模块形成)
   - 教学段(X0~40)1 个:安全缺口,摔下可爬回(坑底有落脚平台)
   - 核心/考试段 1~2 个:必须"起跳→空中按Shift"通过,失败=摔死或撞陷阱
   - 缺口两侧边缘对齐 20px 网格;台灯前后 5 格内不得有缺口(防重生连死)
4. 陷阱位置/类型不变;抽平台不得删除台灯、日记桌、spawn。

【硬约束】
- 缺口宽度统一 120px(6格),不得出现 100px 或 140px(平跳实测5.25格=105px必掉;跳冲上限7~8格=140~160px刚好够)
- 倒序映射不得错:ch2_lv4 必须是一章关1(家暴/老屋)布局
- 每个 ch2 JSON 放地图编辑器里能打开、能从头跑到关底

【验收】
① 自测:从 ch1 一路玩到 ch2_lv1(章级过场:闭眼→黑屏大字→睁眼→赠予演出);
  ch2 每关:平跳掉进 120px 缺口、站冲也掉、起跳→空中 Shift 能过;关底日记日期先行清晰可见;
  光片飞入 HUD 点亮 ◆;ch2_lv4 集齐4片后不翻页(控制台打印 unlock_pending)。
② 教我验证:告诉我"二章从右往左打,大缺口平跳和站冲都过不去,必须跳起来空中按Shift;
  每关日记日期分别是1999/1997/1996/1993;第4关集齐四片不翻页"。
③ 未完成清单:缺口位置分布/日记文案哪些是占位。④ 等我说"通过"。
```

---

## 提示词 C:ch2 占位素材生成(陈洒,10 分钟,可最先做)

```
【项目锚点】(同上,省略)
目录:只改【production/, assets/placeholder/】

【任务】
用 production/gen_placeholder_images.py 的现有函数(先读脚本,复用,不重写生成器):
1. placeholder_bg_ch2_lv1~4_far/mid.png:复制一章对应占位背景,整体压暗+去饱和50%+加碎裂噪点
   (一眼区分一章,又保持布局一致);尺寸与一章对应关完全一致
   (lv1 far 640×360 / lv2 far 800×360 / lv3、lv4 far 1280×360 / mid 均 800×360;
   lv3/lv4 有 mid_a/mid_b 两张)。
2. placeholder_lut_ch2.png:256×16 LUT 格式,破败灰基调(降饱和约40%、亮度×0.9、轻微冷偏移)。
   先读 assets/shaders/lut_color_grading.gdshader 确认 LUT 采样格式再生成,格式错=全屏花。
3. 在 production/session-state/asset-replacement.md 台账登记:上述均为占位,
   真素材=AI 转换一章背景(D-535)+ 真 lut_ch2,到位后改 JSON 路径即可(附录E 替换约定第2条)。

【验收】F5 进 ch2 任意关(提示词B完成后):背景明显比一章破败灰暗,整体色调统一偏灰;
陷阱/角色仍清晰可辨(LUT 只做基调,不死灰)。
```

---

## 执行顺序建议

1. **提示词 C**(占位素材,10 分钟,不挡任何人)
2. **提示词 A**(LUT 键,15 分钟)
3. 补完一章 lv3/lv4(你正在进行中)
4. **提示词 B**(二章 4 关,主体工作量)
5. 串联验收:ch1 全程 → 章级过场 → ch2 全程 → unlock_pending 打印,即里程碑②前半达成

## 数值速查(20px 体系,提示词内已含,单独备查)

| 项 | 值 | 出处 |
|---|---|---|
| 冲刺距离 | 3格 = 60px(400px/s×0.15s) | 任务9 已实现 |
| 二章破败缺口 | 6格 = **120px** | 平跳105px必掉 |
| 平跳实测横距 | 5.25格 = 105px | 附录B 白盒实测 |
| 跳+空中冲刺上限 | 7~8格 = 140~160px | D-203 |
| 台灯安全区 | 前后 5 格 = 100px 无动态陷阱/缺口 | 任务4 配套规则 |
| bg_ch2 尺寸 | 与一章对应关相同(高360) | 附录E |
